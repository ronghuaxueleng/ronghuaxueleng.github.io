<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>白云雅阁</title>
  <subtitle>读懂别人的故事，写好自己的人生</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://ronghuaxueleng.github.io/"/>
  <updated>2017-11-09T03:28:14.562Z</updated>
  <id>http://ronghuaxueleng.github.io/</id>
  
  <author>
    <name>曹强</name>
    <email>caoqianghappy@126.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Shadow DOM</title>
    <link href="http://ronghuaxueleng.github.io/2017/11/09/shadowdom/"/>
    <id>http://ronghuaxueleng.github.io/2017/11/09/shadowdom/</id>
    <published>2017-11-08T16:00:00.000Z</published>
    <updated>2017-11-09T03:28:14.562Z</updated>
    
    <content type="html"><![CDATA[<p><img src="http://7vikhl.com1.z0.glb.clouddn.com/shadow-dom.png" alt=""><br>Shadow DOM 是四大网络组件标准之一，提供 CSS 作用域、DOM 封装和组合(Composition) 等优势，构建自定义元素。</p>
<a id="more"></a>
<h2 id="什么是-shadow-dom"><a href="#什么是-Shadow-DOM" class="headerlink" title="什么是 Shadow DOM"></a>什么是 Shadow DOM</h2><p>浏览器在解析网页文档的时候，会将 HTML 标签转换成节点，为了保持页面的层次结构，这些节点构成一个节点树，这个结构就是 DOM（文档对象模型）。DOM 将文档作为一个结构化的节点组以及包含属性和方法的对象，是 HTML 的编程接口，我们通常会使用 JavaScript 来访问操纵 DOM。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> header = <span class="built_in">document</span>.createElement(<span class="string">'header'</span>);</div><div class="line"><span class="keyword">const</span> h1 = <span class="built_in">document</span>.createElement(<span class="string">'h1'</span>);</div><div class="line">h1.textContent = <span class="string">'Hello world!'</span>;</div><div class="line">header.appendChild(h1);</div><div class="line"><span class="built_in">document</span>.body.appendChild(header);</div></pre></td></tr></table></figure></p>
<p>这段 JavaScript 代码执行后会生成如下的 DOM 结构：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">header</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Hello DOM<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></div><div class="line">  <span class="tag">&lt;/<span class="name">header</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>那什么是 Shadow DOM 呢❓<br>Shadow DOM 为 Web 组件中的 DOM 和 CSS 提供了封装。Shadow DOM 使得这些东西与主文档的 DOM 保持分离。Shadow DOM 与普通的 DOM 的区别是：<br>👉 通常创建新的 DOM 节点附加到其他 DOM 元素上，就会成为其子节点（元素）；但借助 Shadow DOM，可以创建一个  DOM 树附加到其他元素上，这个 DOM 树并非其子节点（元素），其拥有自身的作用域范围，这个 DOM 树称为：<strong><code>shadwo tree</code></strong>。被附加的元素称为 <strong><code>shadow host</code></strong>，这个 DOM 树称为 <strong><code>shadow root</code></strong>。</p>
<blockquote>
<p>You can think of shadow DOM as a scoped subtree inside your element.</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> header = <span class="built_in">document</span>.createElement(<span class="string">'header'</span>);</div><div class="line"><span class="keyword">const</span> shadowRoot = header.attachShadow(&#123;<span class="attr">mode</span>: <span class="string">'open'</span>&#125;);</div><div class="line">shadowRoot.innerHTML = <span class="string">'&lt;h1&gt;Hello Shadow DOM&lt;/h1&gt;'</span>; <span class="comment">// Could also use appendChild().</span></div><div class="line"></div><div class="line"><span class="comment">// header.shadowRoot === shadowRoot</span></div><div class="line"><span class="comment">// shadowRoot.host === header</span></div></pre></td></tr></table></figure>
<ul>
<li>为元素创建 shadow DOM，调用 <code>element.attachShadow()</code></li>
<li>通过 <code>shadowRoot</code> 属性获取元素附加的 Shadow DOM</li>
<li>通过 shadow root 的 <code>host</code> 属性获取其附属的元素</li>
</ul>
<p>❗️<strong>注意：</strong>并非所有元素都可以调用 <code>element.attachShadow()</code> 为其创建添加 Shadow DOM，因为有些元素浏览器已经为其添加了 Shadow DOM，例如 <code>&lt;textarea&gt;</code> <code>&lt;input&gt;</code>；而还有些元素为其添加 Shadow DOM 是没有意义的，例如 <code>&lt;img&gt;</code>。</p>
<figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">document</span><span class="selector-class">.createElement</span>(<span class="string">'input'</span>)<span class="selector-class">.attachShadow</span>(&#123;<span class="attribute">mode</span>: <span class="string">'open'</span>&#125;);</div><div class="line"><span class="comment">// Error. `&lt;input&gt;` cannot host shadow dom.</span></div></pre></td></tr></table></figure>
<p>在创建自定义元素时，Shadow DOM 尤其有用。使用 Shadow DOM 来分隔元素的 HTML、CSS 和 JS，从而生成一个组件。<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line">window.customElements.define(<span class="symbol">'x</span>-tag', <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">HTMLElement</span> </span>&#123;</div><div class="line">  constructor() &#123;</div><div class="line">    <span class="keyword">super</span>()</div><div class="line"></div><div class="line">    let shadowRoot = <span class="keyword">this</span>.attachShadow(&#123;mode: <span class="symbol">'ope</span>n'&#125;)</div><div class="line">    shadowRoot.innerHTML = `</div><div class="line">    &lt;style&gt;</div><div class="line">    b &#123;</div><div class="line">      color: red;</div><div class="line">    &#125;</div><div class="line">    &lt;/style&gt;</div><div class="line">    &lt;b&gt;<span class="type">Hello</span> world&lt;/b&gt;</div><div class="line">    `</div><div class="line">  &#125;</div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<p>注意 Shadow DOM 中的 CSS 规则的作用域仅限于 <code>&lt;x-tag&gt;</code>。</p>
<h2 id="组合和-slot"><a href="#组合和-slot" class="headerlink" title="组合和 slot"></a>组合和 slot</h2><p>组合是 shadow DOM 最难理解的功能之一，但可以说是最重要的功能。</p>
<h3 id="light-dom-和-shadow-dom"><a href="#light-DOM-和-shadow-DOM" class="headerlink" title="light DOM 和 shadow DOM"></a>light DOM 和 shadow DOM</h3><p><strong>light DOM</strong><br>使用组件的用户编写的标记，DOM 元素的实际子元素，该子元素不在组件的 shadow DOM 之内，这就是 light DOM。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">is</span>=<span class="string">"better-button"</span>&gt;</span></div><div class="line">  <span class="comment">&lt;!-- the image and span are better-button's light DOM --&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"gear.svg"</span> <span class="attr">slot</span>=<span class="string">"icon"</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>Settings<span class="tag">&lt;/<span class="name">span</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">button</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>其中 <code>image</code> 和 <code>span</code> 即是 <code>button</code> 的 light DOM。</p>
<p><strong>shadow DOM</strong><br>组件作者编写的标记，它定义组件的内部结构，作用域 CSS 等<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">#shadow-root</div><div class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="undefined">...</span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">slot</span> <span class="attr">name</span>=<span class="string">"icon"</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"wrapper"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">slot</span>&gt;</span>Button<span class="tag">&lt;/<span class="name">slot</span>&gt;</span></div><div class="line">  <span class="tag">&lt;/<span class="name">span</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p><strong>Flattened DOM tree</strong><br>浏览器将用户的 light DOM 分布到 shadow DOM 中，渲染成一颗 DOM 树，这个过程称为 <strong><code>flattening the tree</code></strong>，这棵树称为扁平树 <strong><code>Flattened DOM tree</code></strong>。 扁平树也是我们在 DevTools 中最终看到的树以及在页面上渲染的结果。<br><img src="http://7vikhl.com1.z0.glb.clouddn.com/FF183E44-E19D-4B24-918B-2DE5067699A4.png" alt=""></p>
<h3 id="元素"><a href="#元素" class="headerlink" title=" 元素"></a><slot> 元素</slot></h3><p>Shadow DOM 使用 <code>&lt;slot&gt;</code> 元素将 light DOM 树组合到 shadow DOM 中，形成一颗 DOM 树。</p>
<p>Slot 相当于是组件内部的占位符，通过定义一个或者多个 <code>&lt;slot&gt;</code>，可以将外部的标记引入到组件 shadow DOM 中进行渲染，相当于“跨域”了 shadow DOM 的边界，这些引入到 shadow DOM 中的元素被称为分布式节点 <strong><code>distributed nodes</code></strong>。注意：slot 实际上并不移动这些分布式节点，它们在 shadow DOM 内部的其他位置进行渲染。</p>
<p>组件可在其 shadow DOM 中定义零个或多个 slot。Slot 可以为空，或者提供回退内容(fallback content)，如果用户不提供 light DOM 内容，slot 会将对其备用内容进行渲染。</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="comment">&lt;!-- Default slot. If there's more than one default slot, the first is used. --&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">slot</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span></div><div class="line"></div><div class="line"><span class="tag">&lt;<span class="name">slot</span>&gt;</span>Fancy button<span class="tag">&lt;/<span class="name">slot</span>&gt;</span> <span class="comment">&lt;!-- default slot with fallback content --&gt;</span></div></pre></td></tr></table></figure>
<p>还可以创建命名 slot (named slots)，使用组件的用户通过名称找到特定的 slot。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">x-tag</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">p</span> <span class="attr">slot</span>=<span class="string">"title"</span>&gt;</span>Custom Elements<span class="tag">&lt;/<span class="name">p</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">x-tag</span>&gt;</span></div><div class="line"></div><div class="line"></div><div class="line">#shadow-root</div><div class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-tag">b</span> &#123;</div><div class="line">    <span class="attribute">color</span>: red;</div><div class="line">  &#125;</div><div class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">b</span>&gt;</span>Hello world<span class="tag">&lt;/<span class="name">b</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">slot</span> <span class="attr">name</span>=<span class="string">"title"</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span></div></pre></td></tr></table></figure></p>
<h2 id="设定样式"><a href="#设定样式" class="headerlink" title="设定样式"></a>设定样式</h2><p>shadow DOM 最有用的功能是作用域 CSS：</p>
<ul>
<li>外部页面中的 CSS 选择器不会应用于组件内部。</li>
<li>组件内部定义的样式也不会渗出，它们的作用域仅限于宿主元素(shadow host)</li>
</ul>
<h3 id="通过选择器设定样式"><a href="#通过选择器设定样式" class="headerlink" title="通过选择器设定样式"></a>通过选择器设定样式</h3><p><strong>使用 <code>:host</code> 为组件为自身设定样式</strong><br>需要注意：外部页面中为组件设定的样式比 <code>:host</code> 规则具有更高的优先级，也就是说，对于组件自身，外部样式优先，用户可以在外部设置组件自身的样式。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">// 外部样式比 :host 优先</div><div class="line">x-tag &#123;</div><div class="line">  border: 1px solid red;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#shadow-root</div><div class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-pseudo">:host</span> &#123;</div><div class="line">    <span class="attribute">border</span>: <span class="number">1px</span> solid green;</div><div class="line">  &#125;</div><div class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p><strong>使用 <code>:host(&lt;selector&gt;)</code> 匹配组件自身不同状态</strong><br>通过 <code>:host(&lt;selector&gt;)</code> 匹配不同状态，对组件自身或者内部节点设定不同的样式<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line">#shadow-root</div><div class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-pseudo">:host(</span><span class="selector-pseudo">:hover)</span> &#123;</div><div class="line"></div><div class="line">  &#125;</div><div class="line">  <span class="selector-pseudo">:host(.red_bg)</span> &#123;</div><div class="line"></div><div class="line">  &#125;</div><div class="line">  <span class="selector-pseudo">:host(.green_bg)</span> &#123;</div><div class="line"></div><div class="line">  &#125;</div><div class="line">  <span class="selector-pseudo">:host(.disable)</span> <span class="selector-tag">button</span> &#123;</div><div class="line"></div><div class="line">  &#125;</div><div class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p><strong>使用 <code>:host-context(&lt;selector&gt;)</code> 匹配任意父级</strong><br>使用 <code>:host-context(&lt;selector&gt;)</code> 主要用于根据组件所在的环境进行主题化（基于情境设定样式），例如设置白天模式，夜晚模式，我们在 <code>&lt;html&gt;</code> 或者 <code>&lt;body&gt;</code> 设定不同的 class，而组件通过 <code>:host-context(&lt;selector&gt;)</code> 就可以匹配到</p>
<figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="params">&lt;body class="darktheme"&gt;</span></div><div class="line">  <span class="params">&lt;fancy-tabs&gt;</span></div><div class="line">    ...</div><div class="line">  <span class="params">&lt;/fancy-tabs&gt;</span></div><div class="line"><span class="params">&lt;/body&gt;</span></div><div class="line"></div><div class="line"><span class="meta">#shadow-root</span></div><div class="line">  :host-context(.darktheme) &#123;</div><div class="line"><span class="symbol">    color:</span> white;</div><div class="line"><span class="symbol">    background:</span> black;</div><div class="line">  &#125;</div></pre></td></tr></table></figure>
<p><strong>使用 <code>::slotted(&lt;compound-selector&gt;)</code> 匹配分布到 <slot> 中的节点</slot></strong><br>使用 <code>::slotted(&lt;compound-selector&gt;)</code> 为分布式节点设定样式<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">name-badge</span>&gt;</span></div><div class="line">  #shadow-root</div><div class="line">    <span class="tag">&lt;<span class="name">h2</span>&gt;</span>Eric Bidelman<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"title"</span>&gt;</span></div><div class="line">      Digital Jedi, <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"company"</span>&gt;</span>Google<span class="tag">&lt;/<span class="name">span</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">span</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">name-badge</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>组件的 shadow DOM 可为用户的 <code>&lt;h2&gt;</code> 和 <code>.title</code> 设定样式：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-pseudo">::slotted(h2)</span> &#123;</div><div class="line">    <span class="attribute">margin</span>: <span class="number">0</span>;</div><div class="line">    <span class="attribute">font-weight</span>: <span class="number">300</span>;</div><div class="line">    <span class="attribute">color</span>: red;</div><div class="line">  &#125;</div><div class="line">  <span class="selector-pseudo">::slotted(.title)</span> &#123;</div><div class="line">     <span class="attribute">color</span>: orange;</div><div class="line">  &#125;</div><div class="line">  <span class="comment">/* DOESN'T WORK (can only select top-level nodes).</span></div><div class="line">  ::slotted(.company),</div><div class="line">  ::slotted(.title .company) &#123;</div><div class="line">    text-transform: uppercase;</div><div class="line">  &#125;</div><div class="line">  */</div><div class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>需要注意的是：<br>① 使用 <code>::slotted(&lt;compound-selector&gt;)</code> 只能为分布节点的<strong>顶级节点</strong>设置样式。<br>② slot 并不会移动 light DOM，light DOM 节点分布到 shadow DOM 中后，slot 会对其 DOM 进行渲染，样式设置，但是节点实际还是留在原处。如果外部对 light DOM 设置了样式，那么外部样式将会覆盖 shadow DOM 中通过 <code>::slotted(&lt;compound-selector&gt;)</code> 设置的样式，具有较高优先级。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="undefined"></span></div><div class="line">  x-tag p &#123;</div><div class="line">    color: red; // 外部样式，具有较高优先级</div><div class="line">  &#125;</div><div class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></div><div class="line"></div><div class="line"><span class="tag">&lt;<span class="name">x-tag</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">p</span> <span class="attr">slot</span>=<span class="string">"title"</span>&gt;</span>Custom Elements<span class="tag">&lt;/<span class="name">p</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">x-tag</span>&gt;</span></div><div class="line"></div><div class="line">#shadow-root</div><div class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-pseudo">::slotted(p)</span> &#123;</div><div class="line">    <span class="attribute">color</span>: green;</div><div class="line">  &#125;</div><div class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></div></pre></td></tr></table></figure></p>
<h3 id="从外部为组件设定样式"><a href="#从外部为组件设定样式" class="headerlink" title="从外部为组件设定样式"></a>从外部为组件设定样式</h3><p>有几种方法可从外部为组件设定样式：最简单的方法是使用标记名称作为选择器</p>
<p><strong>为组件自身设定样式</strong><br>从外部为组件自身设定样式最简单的方法，就是使用组件标签名称作为选择器，设置 CSS 样式。<br><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">x-<span class="keyword">tag</span> <span class="title">&#123;</span></div><div class="line"></div><div class="line">&#125;</div><div class="line">x-tag:hover &#123;</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>注意：<strong>外部样式总是优先于在 shadow DOM 中定义的样式。</strong>例如，如果用户在组件外部编写：<code>fancy-tabs { width: 500px; }</code>，它将优先于组件的规则：<code>:host { width: 650px;}</code>。</p>
<p><strong>为组件内部元素设定样式</strong><br>在组件外部，我们不能直接通过选择器对组件内的元素设置样式。组件提供自定义属性 <strong>custom properties</strong>，相当于是样式占位符，外部通过对自定义属性 <strong>custom properties</strong> 来调整组件内部元素的样式。</p>
<p>例如，<code>&lt;fancy-tabs&gt;</code> 可让用户替换背景颜色：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment">&lt;!-- main page --&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">  <span class="selector-tag">fancy-tabs</span> &#123;</div><div class="line">    <span class="attribute">margin-bottom</span>: <span class="number">32px</span>;</div><div class="line">    <span class="attribute">--fancy-tabs-bg</span>: black;</div><div class="line">  &#125;</div><div class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">fancy-tabs</span> <span class="attr">background</span>&gt;</span>...<span class="tag">&lt;/<span class="name">fancy-tabs</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>在其 shadow DOM 内部：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="selector-pseudo">:host(</span><span class="selector-attr">[background]</span>) &#123;</div><div class="line">  <span class="attribute">background</span>: <span class="built_in">var</span>(--fancy-tabs-bg, #9E9E9E);</div><div class="line">  <span class="attribute">border-radius</span>: <span class="number">10px</span>;</div><div class="line">  <span class="attribute">padding</span>: <span class="number">10px</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>在本例中，该组件将使用 <code>black</code> 作为背景值，因为用户指定了该值。 否则背景颜色将采用默认值 <code>#9E9E9E</code>。</p>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="避免在组件中创建-closed-shadow-root"><a href="#避免在组件中创建-closed-shadow-root" class="headerlink" title="避免在组件中创建 closed shadow root"></a>避免在组件中创建 closed shadow root</h3><p>shadow DOM 的另一情况称为<strong>闭合</strong>模式。创建closed shadow DOM tree 后，在外部无法通过 JavaScript 访问组件的内部 DOM。这与 <code>&lt;video&gt;</code> 等原生元素工作方式类似，JavaScript 无法访问 <code>&lt;video&gt;</code> 的 shadow DOM，因为浏览器使用closed shadow root来实现。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> div = <span class="built_in">document</span>.createElement(<span class="string">'div'</span>);</div><div class="line"><span class="keyword">const</span> shadowRoot = div.attachShadow(&#123;<span class="attr">mode</span>: <span class="string">'closed'</span>&#125;); <span class="comment">// close shadow tree</span></div><div class="line"><span class="comment">// div.shadowRoot === null</span></div><div class="line"><span class="comment">// shadowRoot.host === div</span></div></pre></td></tr></table></figure>
<h3 id="关于-slot-的-api"><a href="#关于-slot-的-API" class="headerlink" title="关于 slot 的 API"></a>关于 slot 的 API</h3><p><strong><code>slotchange</code> 事件</strong></p>
<p>当 slot 的分布式节点发生变化时，<code>slotchange</code> 事件会触发。例如，当用户从 light DOM 中添加/移除子项时。</p>
<figure class="highlight typescript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> slot = <span class="keyword">this</span>.shadowRoot.querySelector(<span class="string">'#slot'</span>);</div><div class="line">slot.addEventListener(<span class="string">'slotchange'</span>, <span class="function"><span class="params">e</span> =&gt;</span> &#123;</div><div class="line">  <span class="built_in">console</span>.log(<span class="string">'light dom children changed!'</span>);</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>注：当组件的实例首次初始化时，<code>slotchange</code> 不触发。</p>
<p><strong>查看 slot 中渲染的元素</strong><br>调用 <code>slot.assignedNodes()</code> 可查看 slot 正在渲染哪些元素；该方法中传入 <code>{flatten: true}</code> 选项将返回 slot 的备用内容fallbcak content（前提是没有分布任何节点）。</p>
<table>
<thead>
<tr>
<th>用法</th>
<th>调用</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>&lt;button is=&quot;better-button&quot;&gt;My button&lt;/button&gt;</code></td>
<td>slot.assignedNodes();</td>
<td>[text]</td>
</tr>
<tr>
<td><code>&lt;button is=&quot;better-button&quot;&gt;&lt;/button&gt;</code></td>
<td>slot.assignedNodes();</td>
<td>[]</td>
</tr>
<tr>
<td><code>&lt;button is=&quot;better-button&quot;&gt;&lt;/button&gt;</code></td>
<td>slot.assignedNodes({flatten: true});</td>
<td>[<b>fallback content</b>]</td>
</tr>
</tbody>
</table>
<p><strong>查看元素渲染在哪个 slot 中</strong><br><code>element.assignedSlot</code> 将告诉您元素分配到组件的哪个 slot。</p>
<h3 id="shadow-dom-事件模型"><a href="#Shadow-DOM-事件模型" class="headerlink" title="Shadow DOM 事件模型"></a>Shadow DOM 事件模型</h3><p>当事件从 shadow DOM 中触发时，其目标 target 为维持 shadow DOM 的封装，不暴露 shadow DOM 中的内容元素，会进行重置 retarget，看起来事件是来自组件本身，而不是组件中 shadow DOM 中的内容元素。</p>
<p>大部分事件都会跨过 shadow DOM 边界，向外传播：</p>
<ul>
<li>聚焦事件：blur、focus、focusin、focusout</li>
<li>鼠标事件：click、dblclick、mousedown、mouseenter、mousemove，等等</li>
<li>滚轮事件：wheel</li>
<li>输入事件：beforeinput、input</li>
<li>键盘事件：keydown、keyup</li>
<li>组合事件：compositionstart、compositionupdate、compositionend</li>
<li>拖放事件：dragstart、drag、dragend、drop，等等</li>
</ul>
<p>对于 <code>mode</code> 为 <code>open</code> 的 shadow DOM，事件回调中，调用 <code>event.composedPath()</code> 将返回事件向上传播经过的节点路径。</p>
<p>对于组件内的自定义事件，不会传播出 shadow DOM，除非事件创建时指定 <code>composed: true</code><br><figure class="highlight dart"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// Inside &lt;fancy-tab&gt; custom element class definition:</span></div><div class="line">selectTab() &#123;</div><div class="line">  <span class="keyword">const</span> tabs = <span class="keyword">this</span>.shadowRoot.<span class="built_in">querySelector</span>(<span class="string">'#tabs'</span>);</div><div class="line">  tabs.dispatchEvent(<span class="keyword">new</span> Event(<span class="string">'tab-select'</span>, &#123;bubbles: <span class="keyword">true</span>, composed: <span class="keyword">true</span>&#125;));</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>如果 <code>composed: false</code>（默认值），用户无法在组件外部侦听到这个自定义事件。<br><figure class="highlight dust"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="xml"><span class="tag">&lt;<span class="name">fancy-tabs</span>&gt;</span><span class="tag">&lt;/<span class="name">fancy-tabs</span>&gt;</span></span></div><div class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></div><div class="line">  <span class="keyword">const</span> tabs = <span class="built_in">document</span>.querySelector(<span class="string">'fancy-tabs'</span>);</div><div class="line">  tabs.addEventListener(<span class="string">'tab-select'</span>, e =&gt; <span class="template-variable">&#123;</span></div><div class="line">    // won't fire <span class="keyword">if</span> `tab-<span class="keyword">select</span>` wasn't created with `composed: true`.</div><div class="line">  &#125;<span class="xml"><span class="undefined">);</span></span></div><div class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div></pre></td></tr></table></figure></p>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p>本文主要是阅读 <strong>Shadow DOM v1: Self-Contained Web Components</strong> 后，理解整理而来。<br>英文原文：<a href="https://developers.google.com/web/fundamentals/web-components/shadowdom" target="_blank" rel="external">Shadow DOM v1: Self-Contained Web Components</a><br>中文翻译：<a href="https://developers.google.com/web/fundamentals/web-components/best-practices?hl=zh-cn" target="_blank" rel="external">Shadow DOM v1：独立的网络组件</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;img src=&quot;http://7vikhl.com1.z0.glb.clouddn.com/shadow-dom.png&quot; alt=&quot;&quot;&gt;&lt;br&gt;Shadow DOM 是四大网络组件标准之一，提供 CSS 作用域、DOM 封装和组合(Composition) 等优势，构建自定义元素。&lt;/p&gt;
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>可视化埋点</title>
    <link href="http://ronghuaxueleng.github.io/2017/10/25/%E5%8F%AF%E8%A7%86%E5%8C%96%E5%9F%8B%E7%82%B9/"/>
    <id>http://ronghuaxueleng.github.io/2017/10/25/可视化埋点/</id>
    <published>2017-10-25T06:18:52.000Z</published>
    <updated>2017-11-09T03:24:58.162Z</updated>
    
    <content type="html"><![CDATA[<h3 id="埋点"><a href="#埋点" class="headerlink" title="埋点"></a>埋点</h3><blockquote>
<p>所谓“埋点”，是数据采集领域（尤其是用户行为数据采集领域）的术语，指的是针对特定用户行为或事件进行捕获、处理和发送的相关技术及其实施过程</p>
</blockquote>
<p><strong><em>埋点的技术实质</em></strong>，是先监听软件应用运行过程中的事件，当需要关注的事件发生时进行判断和捕获，然后获取必要的上下文信息，最后将信息整理后发送至服务器端。所监听的事件，通常由操作系统、浏览器、APP框架等平台提供，也可以在基础事件之上进行触发条件的自定义（如点击某一个特定按钮）。一般情况下，埋点可以通过监测分析工具提供的SDK来进行编程实现。<br><strong><em>埋点的业务意义显而易见</em></strong>，即帮助定义和获取分析人员真正需要的业务数据及其附带信息。在不同场景下，业务人员关注的信息和角度可能不同。典型的应用场景有面向数字营销领域的分析，以及面向产品运营领域的分析。前者注重来源渠道和广告效果，后者更在意产品本身流程和体验的优化。两者各有侧重，也可以有一些交叉。所以，对于不同的项目和分析目的，应当设计不同的埋点方案。</p>
<p>近年来，<em>埋点的方法论上也出现了一些业界新趋势</em>，主要分为一下三种：</p>
<ul>
<li><strong><em>代码埋点</em></strong> 在需要埋点的节点直接调用接口上传数据，代表方友盟。</li>
<li><strong><em>可视化埋点</em></strong> 通过可视化工具配置采集节点，在前端自动解析配置并上报埋点数据，代表方案是已经开源的<a href="https://github.com/mixpanel" target="_blank" rel="external">Mixpanel</a>。</li>
<li><strong><em>无埋点</em></strong> 并不是没有埋点，而是在前端采集所有数据并上报，然后在后端通过计算过滤出有用的数据，代表是国内的GrowingIO。</li>
</ul>
<p>三种埋点的对比：</p>
<table>
<thead>
<tr>
<th>埋点方式</th>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>代码埋点</td>
<td>技术实现难度低，数据实时性好</td>
<td>开发效率低，难于维护，数据准确性依赖于埋点的正确性，APP端依赖于发版，不具备热更新能力；web端热更新能力差。</td>
</tr>
<tr>
<td>可视化埋点</td>
<td>开发效率高，易于维护</td>
<td>技术实现较为复杂，目前好的可视化埋点产品都是收费的</td>
</tr>
<tr>
<td>无埋点</td>
<td>易于维护，数据全面</td>
<td>牺牲部分实时性，技术实现较为复杂</td>
</tr>
</tbody>
</table>
<p>Mixpanel主要流程如下：</p>
<ol>
<li>设置要埋点的网址-&gt;生成token</li>
<li>在宿主页面插入代码</li>
<li>在mixpanel页面点击进入宿主页面(<font color="red">下面这种方式在单页面应用中可能存在问题</font>)<br>a：在宿主URL添加上一段查询hash代码<br>b：获取hash代码解析出state并保存到sessionStorage<br>c：删除宿主URL后的hash代码<br>d：初始化<br>e：判断是否是在编辑状态下</li>
</ol>
<p>下面是实现的简单流程图：</p>
<div id="flowchart-0" class="flow-chart"></div>


<p>参考文档：<br><a href="http://help.zhugeio.com/hc/kb/article/1068533/" target="_blank" rel="external">诸葛IO的JS可视化埋点-使用指南</a><br><a href="http://help.zhugeio.com/hc/kb/article/1060006/" target="_blank" rel="external">诸葛IO的JS全埋点接入文档</a><br>[诸葛IO文档][4]</p>
<p>  [4]: <a href="https://docs.zhugeio.com/quickstart/" target="_blank" rel="external">https://docs.zhugeio.com/quickstart/</a><script src="https://cdn.bootcss.com/raphael/2.2.7/raphael.min.js"></script><script src="https://cdn.bootcss.com/flowchart/1.7.0/flowchart.min.js"></script><textarea id="flowchart-0-code" style="display: none">start=>start: 加载JS
cond=>condition: 判断sessionStorage是否存在指定key
estep1=>operation: 加载editor.js
estep2=>operation: 显示添加埋点的导航栏
estep3=>operation: 点击添加按钮开始埋点
estep4=>operation: 点击要埋点的元素
estep5=>operation: 弹出配置对话框进行配置
estep6=>operation: 点击保存配置完毕
estep7=>operation: 点击退出按钮退出编辑
nstep1=>operation: 从服务器获取埋点数据
nstep2=>operation: 在页面找到对应的元素并绑定事件
start->cond
cond(yes)->estep1->estep2->estep3->estep4->estep5->estep6->estep7->estep3
cond(no)->nstep1->nstep2</textarea><textarea id="flowchart-0-options" style="display: none">{"theme":"simple","scale":1,"line-width":2,"line-length":50,"text-margin":10,"font-size":12}</textarea><script>  var code = document.getElementById("flowchart-0-code").value;  var options = JSON.parse(decodeURIComponent(document.getElementById("flowchart-0-options").value));  var diagram = flowchart.parse(code);  diagram.drawSVG("flowchart-0", options);</script></p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;埋点&quot;&gt;&lt;a href=&quot;#埋点&quot; class=&quot;headerlink&quot; title=&quot;埋点&quot;&gt;&lt;/a&gt;埋点&lt;/h3&gt;&lt;blockquote&gt;
&lt;p&gt;所谓“埋点”，是数据采集领域（尤其是用户行为数据采集领域）的术语，指的是针对特定用户行为或事件进行捕获、处理和发送
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="埋点,可视化,Mixpanel" scheme="http://ronghuaxueleng.github.io/tags/%E5%9F%8B%E7%82%B9-%E5%8F%AF%E8%A7%86%E5%8C%96-Mixpanel/"/>
    
  </entry>
  
  <entry>
    <title>MVC</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(6)%EF%BC%9AMVC/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(6)：MVC/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:53:47.040Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>原文地址：[PHP设计模式(六)：MVC][1]</p>
</blockquote>
<h2 id="introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>20世纪80年代，计算机发展迅速，编程技术也日益分化。桌面应用编程，也逐渐出现了用户图形界面和程序逻辑分离的程序设计。到了90年代，web的出现更是让这种程序设计模式得以延续。<br>这种设计模式便是<a href="https://en.wikipedia.org/wiki/Model-view-controller" target="_blank" rel="external">MVC(Model-View-Control)</a>，除了MVC，还有MVC的变种，如<a href="https://en.wikipedia.org/wiki/Model-view-viewmodel" target="_blank" rel="external">MVVM(Model-View-View-Model)</a>等。</p>
<h2 id="mvc"><a href="#MVC" class="headerlink" title="MVC"></a>MVC</h2><p>回到80年代的桌面应用编程，当时面向对象的编程设计模式（见<a href="http://csprojectedu.com/2016/02/22/PHPDesignPatterns-1/" target="_blank" rel="external">PHP设计模式(一)：基础编程模式</a>）兴起，程序员将桌面应用分割成两个大的对象：领域对象(domain objects)和可视对象(presentation objects)。领域对象是对现实事物的抽象模型，可视对象是对用户界面部分的抽象模型。<br>后来人们发现，只有领域对象和可视对象是不够的，特别是在复杂的业务中。根据<a href="http://csprojectedu.com/2016/02/26/PHPDesignPatterns-3/" target="_blank" rel="external">PHP设计模式(三)：封装</a>中介绍的设计原则，在面向对象程序设计中，类和类之间的访问、交互和更新应该是通过Accessors和Mutators。<br>那么如果操作领域对象呢？人们引入了控制器(controller)的对象，通过控制器来操作领域模型。<br>到此，MVC模型逐渐稳定下来，用户通过可视对象操作控制器对象，控制器对象再去操作领域对象。</p>
<h2 id="mvc中的设计模式"><a href="#MVC中的设计模式" class="headerlink" title="MVC中的设计模式"></a>MVC中的设计模式</h2><p>上面介绍的MVC属于抽象度比较高的设计模式，在实际编程中，需要遵守下面的设计模式。</p>
<h3 id="基于接口去编程"><a href="#基于接口去编程" class="headerlink" title="基于接口去编程"></a>基于接口去编程</h3><p>基于接口去编程的好处就是分离设计和实现，这一点我们在<a href="http://csprojectedu.com/2016/02/24/PHPDesignPatterns-2/" target="_blank" rel="external">PHP设计模式(二)：抽象类和接口</a>已经介绍过了，下面我们举一个实际的例子来说明这个设计的好处。</p>
<pre><code>&lt;?php
abstract class Animal {
  protected $name;
  abstract protected function eatFish();
  abstract protected function eatMoss();
  public function eat() {
    if ($this-&gt;eatFish()) {
      echo $this-&gt;name . &quot; can eat fish.\n&quot;;
    }
    if ($this-&gt;eatMoss()) {
      echo $this-&gt;name . &quot; can eat moss.\n&quot;;
    }
  }
}
?&gt;
</code></pre><p>我们创建一个鲸鱼类：</p>
<pre><code>&lt;?php
include_once(&apos;Animal.php&apos;);
class Whale extends Animal {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eatFish() {
    return TRUE;
  }
  public function eatMoss() {
    return FALSE;
  }
}

$whale = new Whale();
$whale-&gt;eat();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Whale.php
Whale eats fish.
</code></pre><p>看上去没什么问题，对吧？我们创建一个鲤鱼类：</p>
<pre><code>&lt;?php
include_once(&apos;Animal.php&apos;);
class Carp extends Animal {
  public function __construct() {
    $this-&gt;name = &quot;Carp&quot;;
  }
  public function eatMoss() {
    return TRUE;
  }
}

$carp = new Carp();
$carp-&gt;eat();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Carp.php
PHP Fatal error: Class Carp contains 1 abstract method and must therefore be
declared abstract or implement the remaining method (Animal::eatFish) in
Carp.php on line 9
</code></pre><p>报错了，对吧？因为我们实现Carp.php的时候故意没有去实现eatFish接口，基于接口的编程设计模式可以在开发期就发现这种逻辑错误。</p>
<h3 id="使用组件而不是继承"><a href="#使用组件而不是继承" class="headerlink" title="使用组件而不是继承"></a>使用组件而不是继承</h3><p>将一个对象拆成更小的对象，这些小的对象成为组件(composition)。尽量使用组件而不是继承的设计模式的意义在于，多种继承之下，子类可能会拥有大量毫无意义的未实现方法。而通过组件的方式，子类可以选择需要的组件。<br>下面给出一个例子：</p>
<pre><code>&lt;?php
abstract class Animal {
  protected $name;
  abstract protected function eatFish();
  abstract protected function eatMoss();
  public function eat() {
    if ($this-&gt;eatFish()) {
      echo $this-&gt;name . &quot; can eat fish.\n&quot;;
    }
    if ($this-&gt;eatMoss()) {
      echo $this-&gt;name . &quot; can eat moss.\n&quot;;
    }
  }
}

class Whale extends Animal {
  protected function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  protected function eatFish() {
    return TRUE;
  }
  protected function eatMoss() {
    return FALSE;
  }
}

class BullWhale extends Whale {
  public function __construct() {
    $this-&gt;name = &quot;Bull Whale&quot;;
  }
  public function getGender() {
    return &quot;Male&quot;;
  }
}
?&gt;
</code></pre><p>这里的BullWhale其实非常冗余，实际的业务模型可能并不需要这么复杂，这就是多重继承的恶果。<br>而组件则不同，通过将行为拆分成不同的部分，又最终子类决定使用哪些组件。<br>下面给出一个例子：</p>
<pre><code>&lt;?php
class Action {
  private $name;
  public function __construct($name) {
    $this-&gt;name = $name;
  }
  public function eat($food) {
    echo $this-&gt;name . &quot; eat &quot;. $food . &quot;.\n&quot;;
  }
}

class Gender {
  private $gender;
  public function __construct($gender) {
    $this-&gt;gender= $gender;
  }
  public function getGender() {
    return $this-&gt;gender;
  }
}

class BullWhale {
  private $action;
  private $gender;
  public function __construct() {
    $this-&gt;action = new Action(&quot;Bull Whale&quot;);
    $this-&gt;gender = new Gender(&quot;Male&quot;);
  }
  public function eatFood($food) {
    $this-&gt;action-&gt;eat($food);
  }
  public function getGender() {
    return $this-&gt;gender-&gt;getGender();
  }
}

$bullWhale = new BullWhale();
$bullWhale-&gt;eatFood(&quot;fish&quot;);
echo $bullWhale-&gt;getGender() . &quot;\n&quot;;
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php BullWhale.php
Bill Whale eat fish.
Male
</code></pre><p>BullWhale由Action和Gender组件构成，不同的类可以选择不同的组件组合，这样就不会造成类冗余了。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>实际编程中，更多的往往是混合架构，如既包含继承，又包含组件的编程设计模式。不过，掌握基本的编程架构设计是一切的基础。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文地址：[PHP设计模式(六)：MVC][1]&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;introduction&quot;&gt;&lt;a href=&quot;#Introduction&quot; class=&quot;headerlink&quot; title=&quot;Introduct
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>多态</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(5)%EF%BC%9A%E5%A4%9A%E6%80%81/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(5)：多态/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:53:52.988Z</updated>
    
    <content type="html"><![CDATA[<h2 id="introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>在[PHP设计模式(四)：继承][2]中我们介绍了继承，利用extends来进行程序设计的方法。<br>在[PHP设计模式(二)：抽象类和接口][3]中我们介绍了接口，事实上也存在利用interface的程序设计方法，那就是多态。<br>和C/C++，Java，Python等语言一样，PHP也支持多态。多态更多是是一种面向对象程序设计的概念，让同一类对象执行同一个接口，但却实现不同的逻辑功能<br>。</p>
<h2 id="多态polymorphism"><a href="#多态-Polymorphism" class="headerlink" title="多态/Polymorphism"></a>多态/Polymorphism</h2><p>还是用动物、鲸鱼和鲤鱼来举例：</p>
<pre><code>&lt;?php
interface IEat {
  function eatFish();
  function eatMoss();
}

class Whale implements IEat {
  public function eatFish() {
    echo &quot;Whale eats fish.\n&quot;;
  }
  public function eatMoss() {
    echo &quot;Whale doesn&apos;t eat fish\n&quot;;
  }
}

class Carp implements IEat {
  public function eatFish() {
    echo &quot;Carp doesn&apos;t eat moss.\n&quot;;
  }
  public function eatMoss() {
    echo &quot;Carp eats moss.\n&quot;;
  }
}

$whale = new Whale();
$whale-&gt;eatFish();
$whale-&gt;eatMoss();
$carp = new Carp();
$carp-&gt;eatFish();
$carp-&gt;eatMoss();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Inheritance.php
Whale eats fish.
Whale doesn&apos;t eat fish.
Carp eats moss.
Carp doesn&apos;t eat moss.
</code></pre><p>注意PHP的函数定义不包含返回值，因此完全可以给不同的接口实现返回不同类型的数据。这一点和C/C++，Java等语言是不同的。此外，返回不同类型的数据，甚至不返回结果，对程序设计来说，会额外增加维护成本，已经和使用接口的初衷不同了（接口为了封装实现，而不同的返回值事实上是需要调用者去理解实现的）。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>合理利用多态对接口进行不同的实现，简化你的编程模型，让代码易于维护。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;introduction&quot;&gt;&lt;a href=&quot;#Introduction&quot; class=&quot;headerlink&quot; title=&quot;Introduction&quot;&gt;&lt;/a&gt;Introduction&lt;/h2&gt;&lt;p&gt;在[PHP设计模式(四)：继承][2]中我们介绍了继承，利用
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>44个 Javascript 变态题解析 (下)</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/javascript/44%E4%B8%AA%20Javascript%20%E5%8F%98%E6%80%81%E9%A2%98%E8%A7%A3%E6%9E%90%20(%E4%B8%8B)/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/javascript/44个 Javascript 变态题解析 (下)/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T09:07:50.356Z</updated>
    
    <content type="html"><![CDATA[<p>承接上篇 <a href="https://github.com/xiaoyu2er/blog/issues/1" target="_blank" rel="external">44个 Javascript 变态题解析 (上)</a></p>
<h2 id="第23题"><a href="#第23题" class="headerlink" title="第23题"></a>第23题</h2><pre><code>[1 &lt; 2 &lt; 3, 3 &lt; 2 &lt; 1]
</code></pre><p>这个题也还可以.</p>
<p>这个题会让人误以为是 <code>2 &gt; 1 &amp;&amp; 2 &lt; 3</code> 其实不是的.</p>
<p>这个题等价于</p>
<pre><code>1 &lt; 2 =&gt; true;
true &lt; 3 =&gt;  1 &lt; 3 =&gt; true;
3 &lt; 2 =&gt; false;
false &lt; 1 =&gt; 0 &lt; 1 =&gt; true;
</code></pre><p>答案是 <code>[true, true]</code></p>
<h2 id="第24题"><a href="#第24题" class="headerlink" title="第24题"></a>第24题</h2><pre><code>// the most classic wtf
2 == [[[2]]]
</code></pre><p>这个题我是猜的. 我猜的 <code>true</code>, 至于为什么…..</p>
<p><code>both objects get converted to strings and in both cases the resulting string
is &quot;2&quot;</code> 我不能信服…</p>
<h2 id="第25题"><a href="#第25题" class="headerlink" title="第25题"></a>第25题</h2><pre><code>3.toString()
3..toString()
3...toString()
</code></pre><p>这个题也挺逗, 我做对了 :) 答案是 <code>error, &#39;3&#39;, error</code></p>
<p>你如果换一个写法就更费解了</p>
<pre><code>var a = 3;
a.toString()
</code></pre><p>这个答案就是 <code>&#39;3&#39;</code>;</p>
<p>为啥呢?</p>
<p>因为在 js 中 <code>1.1</code>, <code>1.</code>, <code>.1</code> 都是合法的数字. 那么在解析 <code>3.toString</code> 的时候这个 <code>.</code><br>到底是属于这个数字还是函数调用呢? 只能是数字, 因为<code>3.</code>合法啊!</p>
<h2 id="第26题"><a href="#第26题" class="headerlink" title="第26题"></a>第26题</h2><pre><code>(function(){
  var x = y = 1;
})();
console.log(y);
console.log(x);
</code></pre><p>答案是 <code>1, error</code></p>
<p>y 被赋值到全局. x 是局部变量. 所以打印 x 的时候会报 <code>ReferenceError</code></p>
<h2 id="第27题"><a href="#第27题" class="headerlink" title="第27题"></a>第27题</h2><pre><code>var a = /123/,
    b = /123/;
a == b
a === b
</code></pre><p>即使正则的字面量一致, 他们也不相等.</p>
<p>答案 <code>false, false</code></p>
<h2 id="第28题"><a href="#第28题" class="headerlink" title="第28题"></a>第28题</h2><pre><code>var a = [1, 2, 3],
    b = [1, 2, 3],
    c = [1, 2, 4]
a ==  b
a === b
a &gt;   c
a &lt;   c
</code></pre><p>字面量相等的数组也不相等.</p>
<p>数组在比较大小的时候按照字典序比较</p>
<p>答案 <code>false, false, false, true</code></p>
<h2 id="第29题"><a href="#第29题" class="headerlink" title="第29题"></a>第29题</h2><pre><code>var a = {}, b = Object.prototype;
[a.prototype === b, Object.getPrototypeOf(a) === b]
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/getPrototypeOf" target="_blank" rel="external">Object/getPrototypeOf</a></li>
</ul>
<p>只有 Function 拥有一个 prototype 的属性. 所以 <code>a.prototype</code> 为 <code>undefined</code>.</p>
<p>而 <code>Object.getPrototypeOf(obj)</code> 返回一个具体对象的原型(该对象的内部<code>[[prototype]]</code>值)</p>
<p>答案 <code>false, true</code></p>
<h2 id="第30题"><a href="#第30题" class="headerlink" title="第30题"></a>第30题</h2><pre><code>function f() {}
var a = f.prototype, b = Object.getPrototypeOf(f);
a === b
</code></pre><p>f.prototype is the object that will become the parent of any objects created<br>with new f while Object.getPrototypeOf returns the parent in the inheritance<br>hierarchy.</p>
<p>f.prototype 是使用使用 new 创建的 f 实例的原型. 而 Object.getPrototypeOf 是 f 函数的原型.</p>
<p>请看:</p>
<pre><code>a === Object.getPrototypeOf(new f()) // true
b === Function.prototype // true
</code></pre><p>答案 <code>false</code></p>
<h2 id="31"><a href="#31" class="headerlink" title="31"></a>31</h2><pre><code>function foo() { }
var oldName = foo.name;
foo.name = &quot;bar&quot;;
[oldName, foo.name]
</code></pre><p>答案 <code>[&#39;foo&#39;, &#39;foo&#39;]</code></p>
<p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/name" target="_blank" rel="external">Function/name</a></li>
</ul>
<p>因为<strong>函数的名字不可变</strong>.</p>
<h2 id="第32题"><a href="#第32题" class="headerlink" title="第32题"></a>第32题</h2><pre><code>&quot;1 2 3&quot;.replace(/\d/g, parseInt)
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Glob" target="_blank" rel="external">String/replace#Specifying_a_function_as_a_parameter</a></li>
</ul>
<p><code>str.replace(regexp|substr, newSubStr|function)</code></p>
<p>如果replace函数传入的第二个参数是函数, 那么这个函数将接受如下参数</p>
<ul>
<li>match 首先是匹配的字符串</li>
<li>p1, p2 …. 然后是正则的分组</li>
<li>offset match 匹配的index</li>
<li>string 整个字符串</li>
</ul>
<p>由于题目中的正则没有分组, 所以等价于问</p>
<pre><code>parseInt(&apos;1&apos;, 0)
parseInt(&apos;2&apos;, 2)
parseInt(&apos;3&apos;, 4)
</code></pre><p>答案: <code>1, NaN, 3</code></p>
<h2 id="第33题"><a href="#第33题" class="headerlink" title="第33题"></a>第33题</h2><pre><code>function f() {}
var parent = Object.getPrototypeOf(f);
f.name // ?
parent.name // ?
typeof eval(f.name) // ?
typeof eval(parent.name) //  ?
</code></pre><p>先说以下答案 <code>&#39;f&#39;, &#39;Empty&#39;, &#39;function&#39;, error</code> 这个答案并不重要…..</p>
<p>这里第一小问和第三小问很简单不解释了.</p>
<p>第二小问笔者在自己的浏览器测试的时候是 <code>&#39;&#39;</code>, 第四问是 <code>&#39;undefined&#39;</code></p>
<p>所以应该是平台相关的. 这里明白 <code>parent === Function.prototype</code> 就好了.</p>
<h2 id="第34题"><a href="#第34题" class="headerlink" title="第34题"></a>第34题</h2><pre><code>var lowerCaseOnly =  /^[a-z]+$/;
[lowerCaseOnly.test(null), lowerCaseOnly.test()]
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/test" target="_blank" rel="external">RegExp/test</a></li>
</ul>
<p>这里 test 函数会将参数转为字符串. <code>&#39;nul&#39;</code>, <code>&#39;undefined&#39;</code> 自然都是全小写了</p>
<p>答案: <code>true, true</code></p>
<h2 id="第35题"><a href="#第35题" class="headerlink" title="第35题"></a>第35题</h2><pre><code>[,,,].join(&quot;, &quot;)
</code></pre><p><code>[,,,] =&gt; [undefined × 3]</code></p>
<p>因为javascript 在定义数组的时候允许最后一个元素后跟一个<code>,</code>, 所以这是个长度为三的稀疏数组(这是长度为三, 并没有 0, 1, 2三个属性哦)</p>
<p>答案: <code>&quot;, , &quot;</code></p>
<h2 id="第36题"><a href="#第36题" class="headerlink" title="第36题"></a>第36题</h2><pre><code>var a = {class: &quot;Animal&quot;, name: &apos;Fido&apos;};
a.class
</code></pre><p>这个题比较流氓.. 因为是浏览器相关, <code>class</code>是个保留字(现在是个关键字了)</p>
<p>所以答案不重要, 重要的是自己在取属性名称的时候尽量避免保留字. 如果使用的话请加引号 <code>a[&#39;class&#39;]</code></p>
<h2 id="第37题"><a href="#第37题" class="headerlink" title="第37题"></a>第37题</h2><pre><code>var a = new Date(&quot;epoch&quot;)
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date" target="_blank" rel="external">Date</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/parse" target="_blank" rel="external">Date/parse</a></li>
</ul>
<p>简单来说, 如果调用 Date 的构造函数传入一个字符串的话需要符合规范, 即满足 Date.parse 的条件.</p>
<p>另外需要注意的是 如果格式错误 构造函数返回的仍是一个Date 的实例 <code>Invalid Date</code>.</p>
<p>答案 <code>Invalid Date</code></p>
<h2 id="第38题"><a href="#第38题" class="headerlink" title="第38题"></a>第38题</h2><pre><code>var a = Function.length,
    b = new Function().length
a === b
</code></pre><p>我们知道一个function(Function 的实例)的 <code>length</code> 属性就是函数签名的参数个数, 所以 b.length == 0.</p>
<p>另外 Function.length 定义为1……</p>
<p>所以不相等…….答案 <code>false</code></p>
<h2 id="第39题"><a href="#第39题" class="headerlink" title="第39题"></a>第39题</h2><pre><code>var a = Date(0);
var b = new Date(0);
var c = new Date();
[a === b, b === c, a === c]
</code></pre><p>还是关于Date 的题, 需要注意的是</p>
<ul>
<li>如果不传参数等价于当前时间. </li>
<li>如果是函数调用 返回一个字符串.</li>
</ul>
<p>答案 <code>false, false, false</code></p>
<h2 id="第40题"><a href="#第40题" class="headerlink" title="第40题"></a>第40题</h2><pre><code>var min = Math.min(), max = Math.max()
min &lt; max
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/min" target="_blank" rel="external">Math/min</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/max" target="_blank" rel="external">Math/max</a></li>
</ul>
<p>有趣的是, Math.min 不传参数返回 <code>Infinity</code>, Math.max 不传参数返回 <code>-Infinity</code> 😆</p>
<p>答案: <code>false</code></p>
<h2 id="第41题"><a href="#第41题" class="headerlink" title="第41题"></a>第41题</h2><pre><code>function captureOne(re, str) {
  var match = re.exec(str);
  return match &amp;&amp; match[1];
}
var numRe  = /num=(\d+)/ig,
    wordRe = /word=(\w+)/i,
    a1 = captureOne(numRe,  &quot;num=1&quot;),
    a2 = captureOne(wordRe, &quot;word=1&quot;),
    a3 = captureOne(numRe,  &quot;NUM=2&quot;),
    a4 = captureOne(wordRe,  &quot;WORD=2&quot;);
[a1 === a2, a3 === a4]
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/exec" target="_blank" rel="external">RegExp/exec</a></li>
</ul>
<p>通俗的讲</p>
<p>因为第一个正则有一个 g 选项 它会‘记忆’他所匹配的内容, 等匹配后他会从上次匹配的索引继续, 而第二个正则不会</p>
<p>举个例子</p>
<pre><code>var myRe = /ab*/g;
var str = &apos;abbcdefabh&apos;;
var myArray;
while ((myArray = myRe.exec(str)) !== null) {
  var msg = &apos;Found &apos; + myArray[0] + &apos;. &apos;;
  msg += &apos;Next match starts at &apos; + myRe.lastIndex;
  console.log(msg);
}
// Found abb. Next match starts at 3
// Found ab. Next match starts at 9
</code></pre><p>所以 a1 = ‘1’; a2 = ‘1’; a3 = null; a4 = ‘2’</p>
<p>答案 <code>[true, false]</code></p>
<h2 id="第42题"><a href="#第42题" class="headerlink" title="第42题"></a>第42题</h2><pre><code>var a = new Date(&quot;2014-03-19&quot;),
    b = new Date(2014, 03, 19);
[a.getDay() === b.getDay(), a.getMonth() === b.getMonth()]
</code></pre><p>这个….</p>
<blockquote>
<p>JavaScript inherits 40 years old design from C: days are 1-indexed in C’s<br>struct tm, but months are 0 indexed. In addition to that, getDay returns the<br>0-indexed day of the week, to get the 1-indexed day of the month you have to<br>use getDate, which doesn’t return a Date object.</p>
</blockquote>
<pre><code>a.getDay()
3
b.getDay()
6
a.getMonth()
2
b.getMonth()
3
</code></pre><p>都是套路!</p>
<p>答案 <code>[false, false]</code></p>
<h2 id="第43题"><a href="#第43题" class="headerlink" title="第43题"></a>第43题</h2><pre><code>if (&apos;http://giftwrapped.com/picture.jpg&apos;.match(&apos;.gif&apos;)) {
  &apos;a gif file&apos;
} else {
  &apos;not a gif file&apos;
}
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/match" target="_blank" rel="external">String/match</a></li>
</ul>
<p>String.prototype.match 接受一个正则, 如果不是, 按照 <code>new RegExp(obj)</code> 转化. 所以 <code>.</code> 并不会转义<br>那么 <code>/gif</code> 就匹配了 /.gif/</p>
<p>答案: <code>&#39;a gif file&#39;</code></p>
<h2 id="第44题"><a href="#第44题" class="headerlink" title="第44题"></a>第44题</h2><pre><code>function foo(a) {
    var a;
    return a;
}
function bar(a) {
    var a = &apos;bye&apos;;
    return a;
}
[foo(&apos;hello&apos;), bar(&apos;hello&apos;)]
</code></pre><p>在两个函数里, a作为参数其实已经声明了, 所以 <code>var a; var a = &#39;bye&#39;</code> 其实就是 <code>a; a =&#39;bye&#39;</code></p>
<p>所以答案 <code>&#39;hello&#39;, &#39;bye&#39;</code></p>
<p>全部结束!</p>
<p>#</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>由于笔者水平有限, 如果解释有误, 还望指出 😄</p>
<p>通过整理, 笔者发现绝大部分题目都是因为自己对于基础知识或者说某个 API 的参数理解偏差才做错的.</p>
<p>笔者的重灾区在原型那一块, 所以这次被虐和整理还是很有意义呀.</p>
<p>笔者相信 坚实的基础是深入编程的前提. 所以基础书还是要常看啊 🐹</p>
<p>最后这些变态题现在看看还变态嘛?</p>
<p>al_Objects/String/replace#Specifying_a_function_as_a_parameter</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;承接上篇 &lt;a href=&quot;https://github.com/xiaoyu2er/blog/issues/1&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;44个 Javascript 变态题解析 (上)&lt;/a&gt;&lt;/p&gt;
&lt;h2 id=&quot;第23题&quot;&gt;&lt;
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>工厂模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(8)%EF%BC%9A%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(8)：工厂模式/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:57:48.471Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>在[PHP设计模式(七)：设计模式分类][2]中我们提到过创建设计模式(Creation<br>patterns)，创建设计模式专注于设计对象(Object)和实例(Instance)的创建过程。<br>创建设计模式包括下面五种设计模式：</p>
<ol>
<li><p>抽象工厂设计模式(Abstract Factory)</p>
</li>
<li><p>生成器模式(Builder)</p>
</li>
<li><p>工厂设计模式(Factory Method)</p>
</li>
<li><p>原型设计模式(Prototype Method)</p>
</li>
<li><p>单例设计模式(Singleton)</p>
</li>
</ol>
<p>当程序逐渐扩展的时候，需要更多的新对象，新对象的创建不应该依赖于创建者，换句话说，新对象的创建过程，不应该依赖调用创建函数的对象。为了减少冗余，增加拓展性，工厂模式就是一种创建新对象时使用的设计模式。</p>
<h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><p>工厂模式，也是五种设计模式中唯一的类的设计模式(Class patterns)，即在类中就能实现的设计模式。<br>听起来挺抽象？对比原型设计模式，这是一种对象设计模式(Object patterns)，通过对象的__clone()方法来实现的设计模式。<br>在工厂模式中，新创建的对象/产品并不依赖于生产它的对象/工厂，新对象和调用者之间是低耦合状态。通常调用者和工厂交互，由工厂来生成新对象，新对象只和工厂有关。</p>
<h2 id="何时使用工厂模式"><a href="#何时使用工厂模式？" class="headerlink" title="何时使用工厂模式？"></a>何时使用工厂模式？</h2><p>简单来说，当需求对类的个数不明确的时候，可以使用工厂模式，如：<br>你需要创建一个在线博物馆，但你并不确切的知道究竟有多少文物，你不可能无限的增加新的文物类，同时对于损毁的文物，你不可能无限的去清理这些类。<br>反过来说，如果你确切的知道类的总量，那么你就没有必要使用工程模式，直接通过继承的方式就能实现好的设计。</p>
<h2 id="example"><a href="#Example" class="headerlink" title="Example"></a>Example</h2><p>还是使用我们惯用的鲸鱼和鲤鱼的例子，现在我们想实现一个海洋馆，目前我们并不确定究竟有多少海洋生物。<br>先是一个抽象的工厂类：</p>
<pre><code>&lt;?php
abstract class Factory {
  protected abstract function create();
  public function factoryStart() {
    return $this-&gt;create();
  }
}
?&gt;
</code></pre><p>然后是两个工厂：鲸鱼工厂和鲤鱼工厂</p>
<pre><code>&lt;?php
class WhaleFactory extends Factory {
  protected function create() {
    $whale = new Whale();
    return $whale-&gt;create();
  }
}
class CarpFactory extends Factory {
  protected function create() {
    $carp = new Carp();
    return $carp-&gt;create();
  }
}
?&gt;
</code></pre><p>然后是抽象的动物接口：</p>
<pre><code>&lt;?php
interface Animal {
  public function create();
}
?&gt;
</code></pre><p>然后是具体的动物类：鲸鱼类和鲤鱼类</p>
<pre><code>&lt;?php
class Whale implements Animal {
  private $name;
  public function create() {
    $this-&gt;name = &quot;Whale&quot;;
    return $this-&gt;name . &quot; is created.\n&quot;;
  }
}
class Carp implements Animal {
  private $name;
  public function create() {
    $this-&gt;name = &quot;Carp&quot;;
    return $this-&gt;name . &quot; is created.\n&quot;;
  }
}
?&gt;
</code></pre><p>下面给出使用工厂创建鲸鱼和鲤鱼的代码：</p>
<pre><code>&lt;?php
$whaleFactory = new WhaleFactory();
echo $whaleFactory-&gt;factoryStart();
$carpFactory = new CarpFactory();
echo $carpFactory-&gt;factoryStart();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>Whale is created.
Carp is created.
</code></pre><p>到这里你是不是觉得，其实直接生成两个类就行了，何必搞这么复杂？别着急，好戏在后面。</p>
<h2 id="修改类的方法"><a href="#修改类的方法" class="headerlink" title="修改类的方法"></a>修改类的方法</h2><p>由于Interface的限制，修改类的方法被限定在了create()方法中，因此可以避免偷懒的程序员新增加的不合理函数。<br>简单修改一下：</p>
<pre><code>&lt;?php
class Whale implements Animal {
  private $name;
  public function create() {
    $this-&gt;name = &quot;Whale&quot;;
    return $this-&gt;name . &quot; is created. Whale eats fish.\n&quot;;
  }
}
class Carp implements Animal {
  private $name;
  public function create() {
    $this-&gt;name = &quot;Carp&quot;;
    return $this-&gt;name . &quot; is created. Carp eats moss.\n&quot;;
  }
}
?&gt;
</code></pre><p>由于对象是由工厂造出来的，外部不可能直接调用或者修改类的实现，类的修改被限定在了类的对外接口上。这样的架构易于扩展。</p>
<h2 id="一个工厂"><a href="#一个工厂" class="headerlink" title="一个工厂"></a>一个工厂</h2><p>工厂模式的灵活，在于可以只拥有一个工厂，却能生产多个类/产品。<br>修改我们的抽象工厂，给create()方法增加animal接口：</p>
<pre><code>&lt;?php
abstract class Factory {
  protected abstract function create(Animal $animal);
  public function factoryStart($animal) {
    return $this-&gt;create($animal);
  }
}
?&gt;
</code></pre><p>然后合并之前的鲸鱼工厂和鲤鱼工厂：</p>
<pre><code>&lt;?php
class AnimalFactory extends Factory {
  protected function create(Animal $animal) {
    return $animal-&gt;create();
  }
}
?&gt;
</code></pre><p>修改使用工厂创建鲸鱼和鲤鱼的代码：</p>
<pre><code>&lt;?php
$animalFactory = new AnimalFactory();
echo $animalFactory-&gt;factoryStart(new Whale());
echo $animalFactory-&gt;factoryStart(new Carp());
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>Whale is created. Whale eats fish.
Carp is created. Carp eats moss.
</code></pre><p>鲸鱼类和鲤鱼类源源不断的从一个工厂中被创建出来了。通过这种设计模式，类的创建过程统一通过一个接口来实现，接口外部并不需要关心类是如何被创建出来的，而接口内部实现也得到了很好的拓展性。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文介绍了工厂设计模式，使用这种设计模式，可以让你通过一个或多个工厂的接口，创建无数新类，调用任意类的方法。由于接口严格定义了新类/产品的形态，因此在维护和<br>拓展的时候，可以省去不少力气。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;在[PHP设计模式(七)：设计模式分类][2]中我们提到过创建设计模式(Creation&lt;br&gt;patterns)，创建设计模式专注于设计对
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>继承</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(4)%EF%BC%9A%E7%BB%A7%E6%89%BF/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(4)：继承/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:54:00.581Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>在[PHP设计模式(二)：抽象类和接口][2]以及[PHP设计模式(三)：封装][3]中，我们已经见过继承，也就是extends关键字。<br>和C/C++，Java，Python等语言一样，PHP也支持继承，而且和其他语言没有什么区别。</p>
<h2 id="继承inheritance"><a href="#继承-Inheritance" class="headerlink" title="继承/Inheritance"></a>继承/Inheritance</h2><p>还是用动物、鲸鱼和鲤鱼来举例：</p>
<pre><code>&lt;?php
abstract class Animal {
  protected $name;

  protected function chew($food) {
    echo $this-&gt;name . &quot; is chewing &quot; . $food . &quot;.\n&quot;;
  }
  protected function digest($food) {
    echo $this-&gt;name . &quot; is digesting &quot; . $food . &quot;.\n&quot;;
  }
}

class Whale extends Animal {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eat($food) {
    $this-&gt;chew($food);
    $this-&gt;digest($food);
  }
}

class Carp extends Animal {
  public function __construct() {
    $this-&gt;name = &quot;Carp&quot;;
  }
  public function eat($food) {
    $this-&gt;chew($food);
    $this-&gt;digest($food);
  }
}

$whale = new Whale();
$whale-&gt;eat(&quot;fish&quot;);
$carp = new Carp();
$carp-&gt;eat(&quot;moss&quot;);
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Inheritance.php
Whale is chewing fish.
Whale is digesting fish.
Carp is chewing moss.
Carp is digesting moss.
</code></pre><p>注意$this在Animal类、Whale类、Carp类中的用法。<br>上面的代码看似常见，实则暗含玄机。对于一个好的程序设计，需要：</p>
<ol>
<li><p>类和类之间应该是低耦合的。</p>
</li>
<li><p>继承通常是继承自抽象类，而不是具体类。</p>
</li>
<li><p>通常直接继承抽象类的具体类只有一层，在抽象类中用protected来限定。</p>
</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>合理的继承对于好的程序设计同样是必不可少的，结合abstract和protected，能让你编写出结构清晰的代码。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;在[PHP设计模式(二)：抽象类和接口][2]以及[PHP设计模式(三)：封装][3]中，我们已经见过继承，也就是extends关键字。&lt;b
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>原型设计模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(9)%EF%BC%9A%E5%8E%9F%E5%9E%8B%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(9)：原型设计模式/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:56:52.270Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>在[PHP设计模式(八)：工厂模式][2]中我们介绍了创建设计模式(Creation<br>patterns)中的工厂模式，下面我们将介绍另一种原型设计模式(Prototype Method)。<br>在PHP中，原型设计模式依靠cloning复制对象来实现。通过cloning构造的对象，将大量节省新对象的构造时间。</p>
<h2 id="何时使用原型设计模式"><a href="#何时使用原型设计模式？" class="headerlink" title="何时使用原型设计模式？"></a>何时使用原型设计模式？</h2><p>简单来说，当你希望根据已有的对象来创建新对象时。<br>为什么会有这种需求？想象一下，你在做细胞分裂的项目，每一个细胞都是一个对象，现在你有一个细胞类，每一个新的细胞都是由这个类生成的，不同的细胞只是内部的状态参数不同。<br>当分裂到第N代的时候，已经和初代大不一样了，你是愿意使用第N代的副本修改一下呢？还是愿意从初代慢慢推演？</p>
<h2 id="example"><a href="#Example" class="headerlink" title="Example"></a>Example</h2><p>PHP提供了内建的__clone()函数以及clone关键字，来实现对象的复制。下面给出一个例子：</p>
<pre><code>&lt;?php
abstract class Cell {
  public $id;
  public $dna;
  abstract function __clone();
}

class WhaleCell extends Cell {
  public function __construct() {
    $this-&gt;id = 1;
    $this-&gt;dna = &quot;ATCG&quot;;
  }
  public function displayDNA() {
    echo $this-&gt;dna . &quot;\n&quot;;
  }
  function __clone() {
    $this-&gt;id = $this-&gt;id + 1;
    if ($this-&gt;id % 3 == 0) {
      $this-&gt;dna = $this-&gt;dna . &quot;AT&quot;;
    }
    if ($this-&gt;id % 5 == 0) {
      $this-&gt;dna = $this-&gt;dna . &quot;CG&quot;;
    }
  }
}

$whaleCell = new WhaleCell();
$whaleCell-&gt;displayDNA();
$whaleCell2 = clone $whaleCell;
$whaleCell2-&gt;displayDNA();
$whaleCell3 = clone $whaleCell2;
$whaleCell3-&gt;displayDNA();
$whaleCell4 = clone $whaleCell3;
$whaleCell4-&gt;displayDNA();
$whaleCell5 = clone $whaleCell4;
$whaleCell5-&gt;displayDNA();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>ATCG
ATCG
ATCGAT
ATCGAT
ATCGATCG
</code></pre><p>程序简单的模拟了DNA的遗传突变，每遗传三代，DNA增加AT，每遗传五代，DNA增加CG。</p>
<h2 id="原型设计模式中的构造函数"><a href="#原型设计模式中的构造函数" class="headerlink" title="原型设计模式中的构造函数"></a>原型设计模式中的构造函数</h2><p>使用clone创建新对象时，并不会触发类的构造函数。这也是在使用原型设计模式中需要注意的一点。clone的底层实现并不是调用类的构造函数来创建一个类，而是直接拷贝一个类的地址空间，生成另一个类。这种直接拷贝也带来了高效。<br>事实上，使用构造函数并不一定是一个好的设计，由于构造函数内的逻辑无法被外部控制，当需要修改一个类构造时的逻辑时，除了修改类的构造函数实现以外，别无他法，这破坏了类的封装。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>原型设计模式带来了另一种创建对象的思路，合理的使用cloning构造对象，将提高程序创建新对象时的效率。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;在[PHP设计模式(八)：工厂模式][2]中我们介绍了创建设计模式(Creation&lt;br&gt;patterns)中的工厂模式，下面我们将介绍另
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>设计模式分类</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(7)%EF%BC%9A%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E5%88%86%E7%B1%BB/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(7)：设计模式分类/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:58:31.736Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>根据目的和范围，设计模式可以分为五类。按照目的分为：创建设计模式，结构设计模式，以及行为设计模式。按照范围分为：类的设计模式，以及对象设计模式。下面分别介绍<br>。</p>
<h2 id="创建设计模式"><a href="#创建设计模式" class="headerlink" title="创建设计模式"></a>创建设计模式</h2><p>创建设计模式(Creational patterns)，用于创建对象时的设计模式。更具体一点，初始化对象流程的设计模式。当程序日益复杂时，需要更加灵活地创建对象，同时减少创建时的依赖。而创建设计模式就是解决此问题的一类设计模式。</p>
<h2 id="结构设计模式"><a href="#结构设计模式" class="headerlink" title="结构设计模式"></a>结构设计模式</h2><p>结构设计模式(Structural patterns)，用于继承和接口时的设计模式。结构设计模式用于新类的函数方法设计，减少不必要的类定义，减少代码的冗余。</p>
<h2 id="行为设计模式"><a href="#行为设计模式" class="headerlink" title="行为设计模式"></a>行为设计模式</h2><p>行为设计模式(Behavioral<br>patterns)，用于方法实现以及对应算法的设计模式，同时也是最复杂的设计模式。行为设计模式不仅仅用于定义类的函数行为，同时也用于不同类之间的协议、通信。</p>
<h2 id="类的设计模式"><a href="#类的设计模式" class="headerlink" title="类的设计模式"></a>类的设计模式</h2><p>类的设计模式(Class patterns)，用于类的具体实现的设计模式。包含了如何设计和定义类，以及父类和子类的设计模式。</p>
<h2 id="对象设计模式"><a href="#对象设计模式" class="headerlink" title="对象设计模式"></a>对象设计模式</h2><p>对象设计模式(Object patterns)，用于对象的设计模式。与类的设计模式不同，对象设计模式主要用于运行期对象的状态改变、动态行为变更等。</p>
<h2 id="为什么会重构"><a href="#为什么会重构？" class="headerlink" title="为什么会重构？"></a>为什么会重构？</h2><p>重构通常是由于现有程序的框架不能很好的适应新需求，虽然可以通过硬编码(hardcode)或者绕过(bypass)的方式来解决一个新需求，但这并不是长久之计。<br>当新需求越来越多时，现在程序会越来越冗余，导致最后的不可维护。</p>
<h2 id="架构和设计模式的区别"><a href="#架构和设计模式的区别" class="headerlink" title="架构和设计模式的区别"></a>架构和设计模式的区别</h2><p>相比架构(frameworks)，设计模式是一些抽象的组成架构的元素，且更为灵活通用。<br>架构更适合解决具体问题，使用架构构建应用程序将更为快捷，不过相比设计模式，架构牺牲了灵活性。</p>
<h2 id="summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>本文介绍了设计模式的分类，后面我们将分别介绍各种设计模式。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;根据目的和范围，设计模式可以分为五类。按照目的分为：创建设计模式，结构设计模式，以及行为设计模式。按照范围分为：类的设计模式，以及对象设计模
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>基础编程模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(1)%EF%BC%9A%E5%9F%BA%E7%A1%80%E7%BC%96%E7%A8%8B%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(1)：基础编程模式/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:44:59.316Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>俗话说，“PHP是世界上最好的语言”，因为PHP什么都能干。但是在PHP编程中，你是否会遇到这样的困惑：明明是相同的需求，但是之前写的代码却并不能重用，稍微修改不满足需求，大改又会让页面变样。<br>是的，由于PHP什么都能干，但是高度灵活性降低了代码的结构性。虽然可以利用三方框架来解决问题，但问题的根本在于缺乏设计模式。<br>本系列文章将由浅入深的介绍各种设计模式。</p>
<h2 id="面向对象编程"><a href="#面向对象编程" class="headerlink" title="面向对象编程"></a>面向对象编程</h2><p>面向对象编程，Object-Oriented Programming(OOP)作为最基本的设计模式并不是什么新鲜的话题，但是大部分新手的PHP编程都是在写流水账，各种拼接字符串，所以这里还是要提一下。<br><a href="https://en.wikipedia.org/wiki/Object-oriented_programming" target="_blank" rel="external">Object-Oriented Programming</a>的概念这里就不说了，毕竟很多人都明白，但是如何在PHP中使用？<br>假设你需要在页面上显示不同的用户类型，如电脑用户、手机用户等，那么你可以将“显示”这件事抽象为一个类，如：</p>
<pre><code>&lt;?php
class ShowAgent {
  private $agent;
  public function __construct() {
    $this-&gt;agent = $_SERVER[&apos;HTTP_USER_AGENT&apos;];
    echo $this-&gt;agent;
  }
}
$showAgent = new ShowAgent();
?&gt;
</code></pre><h2 id="调试技巧"><a href="#调试技巧" class="headerlink" title="调试技巧"></a>调试技巧</h2><p>在很多PHP默认环境中，调试功能是关闭的。打开调试功能又需要配置php.ini文件，其实有一个简单的方法：</p>
<pre><code>&lt;?php
ini_set(&quot;display_errors&quot;, &quot;1&quot;);
ERROR_REPORTING(E_ALL);
?&gt;
</code></pre><p>将这段代码加入到你的代码中，甚至可以require或者include进去，方便调试。</p>
<h2 id="流水账编程"><a href="#流水账编程" class="headerlink" title="流水账编程"></a>流水账编程</h2><p>这里列出流水账编程，并不是让你学习，而是指出何种编程不推荐使用：</p>
<pre><code>&lt;?php
$total = &quot;Total number is &quot;;
$number = &quot;6&quot;;
$totalNumber = $total.$number;
echo $totalNumber;
?&gt;
</code></pre><p>这段代码并没有错，但是以后再也无法重用了，对吧？每次遇到相同问题，你都需要反复拼接。</p>
<h2 id="面向过程编程"><a href="#面向过程编程" class="headerlink" title="面向过程编程"></a>面向过程编程</h2><p>面向过程编程曾经很流行，缺点也是无法维护，例如：</p>
<pre><code>&lt;?php
function showTotal($total, $number) {
  $totalNumber = $total.$number;
  echo $totalNumber;
}
showTotal(&quot;Total number is&quot;, &quot;6&quot;);
?&gt;
</code></pre><p>这段代码同样没有错，但是时间久了，由于缺乏类的概念，showTotal在各种应用场景缺乏灵活性，你还是需要重写代码。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>转变编程的思维需要花费的时间是很长的，但是记住：算法提高程序运行的速度，而设计模式提高编程的速度。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;俗话说，“PHP是世界上最好的语言”，因为PHP什么都能干。但是在PHP编程中，你是否会遇到这样的困惑：明明是相同的需求，但是之前写的代码却
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>抽象类和接口</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(2)%EF%BC%9A%E6%8A%BD%E8%B1%A1%E7%B1%BB%E5%92%8C%E6%8E%A5%E5%8F%A3/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(2)：抽象类和接口/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:48:50.518Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>对于编程来说，对事物的抽象是一个老生常谈的话题，抽象问题更利于面向对象编程以及程序设计模式。<br>和C/C++，Java，Python等语言一样，PHP也支持面向对象，但是又有略微区别，如PHP支持在接口中定义常量，但是不支持抽象变量。</p>
<h2 id="抽象abstraction"><a href="#抽象-Abstraction" class="headerlink" title="抽象/Abstraction"></a>抽象/Abstraction</h2><blockquote>
<p>对事物的抽象是指，区别两个不同事物之间的本质特征，这两个事物应该在某个视角上有明确的区分界限。</p>
</blockquote>
<p>如，鲸鱼和鲤鱼，这两个事物在动物的视角上，有明确的区分界限，属于不同的动物；但是在水生动物的视角上，他们属于同一种动物的抽象。<br>合理的对问题进行抽象，构造模型，将更容易通过编程来解决问题。<br>记住：抽象是编程解决问题的基础，越复杂的问题，越需要一开始就对问题进行抽象，而不是直接写代码。</p>
<h2 id="抽象类abstract-class"><a href="#抽象类-Abstract-Class" class="headerlink" title="抽象类/Abstract Class"></a>抽象类/Abstract Class</h2><p>抽象类是一个编程概念，PHP中叫Abstract Classes。在设计模式中，抽象类不能够被实例化/初始化，但是可以依靠具体类的继承来实现。<br>有点抽象，对吧？用代码来解释：</p>
<pre><code>&lt;?php
abstract class Animal {
  public $name;
  abstract public function eat($food);
}
?&gt;
</code></pre><p>定义了动物这个抽象类，动物的属性是名字name，然后有一个方法是吃食物eat food。<br>为什么动物是抽象类？因为动物这个物种并不是一个存在于自然界的东西，它是人类脑海里抽象出的东西。存在自然界的是鲸鱼和鲤鱼这样的确定性动物。<br>比如鲸鱼的概念，应该是属于动物，继承Animal类，我们定义鲸鱼这个类以及吃东西的方法：</p>
<pre><code>&lt;?php
class Whale extends Animal {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eat($food) {
    echo $this-&gt;name . &quot; eat &quot; . $food . &quot;.\n&quot;;
  }
}
?&gt;
</code></pre><p>现在我们可以初始鲸鱼类，并且调用吃的方法了：</p>
<pre><code>&lt;?php
  $whale = new Whale();
  $whale-&gt;eat(&quot;fish&quot;);
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Whale.php
Whale eat fish.
</code></pre><h2 id="接口interface"><a href="#接口-Interface" class="headerlink" title="接口/Interface"></a>接口/Interface</h2><p>PHP也支持面向过程编程概念中的接口，下面同样用鲸鱼的例子来讲述：</p>
<pre><code>&lt;?php
interface IAction {
  public function eat($food);
  public function swim();
}
?&gt;
</code></pre><p>同样定义一个鲸鱼类，来实现上述接口：</p>
<pre><code>&lt;?php
class Whale implements IAction {
  public function eat($food) {
    echo &quot;Whale eat &quot; . $food . &quot;\n.&quot;;
  }
  public swim() {
    echo &quot;Whale is swimming.\n&quot;;
  }
}
?&gt;
</code></pre><p>现在我们可以初始鲸鱼类，并且调用吃的方法了：</p>
<pre><code>&lt;?php
  $whale = new Whale();
  $whale-&gt;eat(&quot;fish&quot;);
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Whale.php
Whale eat fish.
</code></pre><h2 id="抽象类vs接口"><a href="#抽象类vs接口" class="headerlink" title="抽象类vs接口"></a>抽象类vs接口</h2><p>上面的抽象类和接口的例子，看上去是不是类似？事实上，对于PHP编程来说，抽象类可以实现的功能，接口也可以实现。<br>抽象类的接口的区别，不在于编程实现，而在于程序设计模式的不同。<br>一般来讲，抽象用于不同的事物，而接口用于事物的行为。<br>如：水生生物是鲸鱼的抽象概念，但是水生生物并不是鲸鱼的行为，吃东西才是鲸鱼的行为。<br>对于大型项目来说，对象都是由基本的抽象类继承实现，而这些类的方法通常都由接口来定义。<br>此外，对于事物属性的更改，建议使用接口，而不是直接赋值或者别的方式，如：</p>
<pre><code>&lt;?php
interface IAction {
  public function eat();
}
class Whale implements IAction {
  public function eat() {
    echo &quot;Whale eat fish.\n&quot;;
  }
}
class Carp implements IAction {
  public function eat() {
    echo &quot;Carp eat moss.\n&quot;;
  }
}

class Observer {
  public function __construct() {
    $whale = new Whale();
    $carp = new Carp();
    $this-&gt;observeEat($whale);
    $this-&gt;observeEat($carp);
  }
  function observeEat(IAction $animal) {
    $animal-&gt;eat();
  }
}
$observer = new observer();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>$ php Observer.php
Whale eat fish.
Carp eat moss.
</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>好的设计模式是严格对问题进行抽象，虽然抽象类和接口对于编程实现来说是类似的，但是对于程序设计模式是不同的。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;对于编程来说，对事物的抽象是一个老生常谈的话题，抽象问题更利于面向对象编程以及程序设计模式。&lt;br&gt;和C/C++，Java，Python等语
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>封装</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(3)%EF%BC%9A%E5%B0%81%E8%A3%85/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(3)：封装/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:54:07.415Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>原文地址：[PHP设计模式(三)：封装][1]</p>
</blockquote>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>面向对象编程中，一切都是对象，对一个对象的封装，也成了面向对象编程中必不可少的部分。<br>和C/C++，Java，Python等语言一样，PHP也支持封装。</p>
<h2 id="封装encapsulation"><a href="#封装-Encapsulation" class="headerlink" title="封装/Encapsulation"></a>封装/Encapsulation</h2><blockquote>
<p>对事物的封装是指，将事物进行抽象后，提供抽象概念的实现的具体方法。</p>
</blockquote>
<p>听起来很拗口，还是举鲸鱼的例子。<br>对于鲸鱼来说，需要吃东西这个行为，吃本身是一个抽象的概念，因为具体到怎么吃，是咀嚼和消化的过程，甚至如何咀嚼和消化也是不可见的。对外部而言，可见的只是吃这一个接口，如何吃、怎么吃，是被封装在了鲸鱼的实现中。甚至可以说，消化系统，被封装到了鲸鱼这个对象中，对外部不可见，仅仅鲸鱼自己可见。</p>
<h2 id="封装方法"><a href="#封装方法" class="headerlink" title="封装方法"></a>封装方法</h2><p>和别的程序设计语言一样，PHP也只是三种封装概念：Private，Protected，Public。</p>
<h3 id="私有private"><a href="#私有-Private" class="headerlink" title="私有/Private"></a>私有/Private</h3><p>私有的概念是，仅仅对象内部可见，外部不可见，如：</p>
<pre><code>&lt;?php
class Whale {
  private $name;
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eat($food) {
    chew($food);
    digest($food);
  }
  private function chew($food) {
    echo &quot;Chewing &quot; . $food . &quot;\n&quot;;
  }
  private function digest($food) {
    echo &quot;Digest &quot; . $food . &quot;\n&quot;;
  }
}
?&gt;
</code></pre><p>name是鲸鱼的私有属性，chew()和digest()是鲸鱼的私有方法，对于其他类来说，都是不可见的。对于现实来说，我们如果只是注重吃，并没有必要去关心鲸鱼是如何去吃的。</p>
<h3 id="保护protected"><a href="#保护-Protected" class="headerlink" title="保护/Protected"></a>保护/Protected</h3><p>保护的概念是，仅仅是自身类和继承类可见，这个关键字的用途主要是防止滥用类的派生，另外三方库编写的时候会用到，防止误用。</p>
<pre><code>&lt;?php
abstract class Animal {
  private $name;
  abstract public function eat($food);
  protected function chew($food) {
    echo &quot;Chewing &quot; . $food . &quot;\n&quot;;
  }
  protected function digest($food) {
    echo &quot;Digest &quot; . $food . &quot;\n&quot;;
  }
}

class Whale extends Animal {
  private $name;
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eat($food) {
    chew($food);
    digest($food);
  }
}
?&gt;
</code></pre><p>鲸鱼类可以通过继承使用动物类的咀嚼和消化方法，但是别的继承鲸鱼类的类就不可以再使用动物类的咀嚼和消化方法了。保护更多是用于面向对象设计，而不是为了编程来实现某个需求。</p>
<h3 id="公共public"><a href="#公共-Public" class="headerlink" title="公共/Public"></a>公共/Public</h3><p>公共的概念就是，任何类、任何事物都可以访问，没有任何限制，这里不再赘述。</p>
<h2 id="getterssetters"><a href="#Getters-Setters" class="headerlink" title="Getters/Setters"></a>Getters/Setters</h2><p>Getters和Setters也叫Accessors和Mutators，在Java/C#等语言中常以get()/set()方法出现。<br>对于这两个东西的争议很大，考虑下面一个类：</p>
<pre><code>&lt;?php
class Price {
  public $priceA;
  public $priceB;
  public $priceC;
  ...
}
?&gt;
</code></pre><p>如果不使用Getters/Setters，我们给Price类赋值和取值一般是这样：</p>
<pre><code>&lt;?php
  $price = new Price();
  $price-&gt;priceA = 1;
  $price-&gt;priceB = 2;
  $price-&gt;priceC = 3;
  ...
  echo $price-&gt;priceA;
  echo $price-&gt;priceB;
  echo $price-&gt;priceC;
  ...
?&gt;
</code></pre><p>但是如果使用了Getters/Setters，Price类将变成这样：</p>
<pre><code>&lt;?php
class Price {
  private $priceA;
  private $priceB;
  private $priceC;
  public function getPriceA() {
    return $this-&gt;priceA;
  }
  public function setPriceA($price) {
    $this-&gt;priceA = $price;
  }
  ...
}
?&gt;
</code></pre><p>这时候赋值将变成这样：</p>
<pre><code>&lt;?php
  $price = new Price();
  $price-&gt;setpriceA(1);
  $price-&gt;setPriceB(2);
  $price-&gt;setPriceC(3);
  ...
  echo $price-&gt;getPriceA();
  echo $price-&gt;getPriceB();
  echo $price-&gt;getPriceC();
  ...
?&gt;
</code></pre><p>是不是感觉需要多敲很多代码？这也是很多程序员不愿意使用get/set的原因，造成了大量的看似无用冗余的代码。<br>为什么叫看似冗余和无用？因为Getters/Setters是编程设计方法，而不是编程实现方法。</p>
<blockquote>
<p>在面向对象程序设计中，类和类之间的访问、交互和更新应该是通过Accessors和Mutators，也就是Getters和Setters来实现。直接访问和<br>修改破坏了类的封装性。</p>
</blockquote>
<p>为什么采用这种设计方式？因为程序设计是对现实问题的抽象，而在编程的工程中程序员扮演的角色往往是上帝。<br>考虑这样一种场景：你朋友要求你改名，决定是否改名的人是你，而不是你朋友。在你的朋友的视觉（也就是你朋友的类），他不能直接去修改你的名字。<br>如果你直接采用非Getters/Setters的设计方法，事实上是程序员扮演的这个上帝修改了现实规则，允许你朋友能够随意更改你的姓名，显然这是不合理的。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>合理的封装对于好的程序设计是必不可少的，虽然什么都是Public也能解决编程问题，但是这不是用程序设计解决问题的思路。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文地址：[PHP设计模式(三)：封装][1]&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;面向对象编程中，一切都是
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>适配器模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/php%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/PHP%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F(10)%EF%BC%9A%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/php设计模式/PHP设计模式(10)：适配器模式/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T07:56:03.613Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>在[PHP设计模式(七)：设计模式分类][2]中我们提到过结构设计模式(Structural<br>patterns)，结构设计模式专注于设计对象(Object)和实例(Instance)的构建、组合过程。<br>结构设计模式包括下面七种设计模式：</p>
<ol>
<li><p>适配器(Adapter)</p>
</li>
<li><p>桥接(Bridge)</p>
</li>
<li><p>合成(Composite)</p>
</li>
<li><p>装饰(Decorator)</p>
</li>
<li><p>外部(Facade)</p>
</li>
<li><p>轻量(Flyweight)</p>
</li>
<li><p>代理(Proxy)</p>
</li>
</ol>
<p>请无视拗口的中文翻译。结构设计模式着重于低耦合、高复用、高可维护性、高拓展性的设计原则。</p>
<h2 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h2><p>适配器模式是一种利用适配器将现有的实现，适配到已有接口的设计模式，最常见的例子就是变压器，将已有的5V输入的电器，通过变压器，适配到220V的电源插座。<br>适配器模式利用[PHP设计模式(四)：继承][3]中我们提到过的继承(inheritance)，以及[PHP设计模式(六)：MVC][4]中我们提到过的组件(composition)来进行模式设计。<br>相比继承，组件可用性高，低耦合，冗余度低，因此推荐采用组件的模式来进行设计。</p>
<h2 id="何时使用适配器模式"><a href="#何时使用适配器模式？" class="headerlink" title="何时使用适配器模式？"></a>何时使用适配器模式？</h2><p>简单来说，当你的实现和需要的接口，都无法修改的时候。<br>例如，你需要给甲方已有的系统做标准的兼容，标准不可修改，甲方的系统也不可修改，这个时候你就需要适配器的设计模式了。<br>对于web编程来说，将你现有的实现，和三方库结合起来，就需要使用适配器模式。</p>
<h2 id="使用继承的适配器模式"><a href="#使用继承的适配器模式" class="headerlink" title="使用继承的适配器模式"></a>使用继承的适配器模式</h2><p>简单来说，就是：</p>
<pre><code>&lt;?php
class Adapter extends YourClass implements OtherInterface
?&gt;
</code></pre><p>还是用前面的鲸鱼和鲤鱼的例子来说明如何使用适配器，假设我们已经实现了鲸鱼类和鲤鱼类：</p>
<pre><code>&lt;?php
class Whale {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eatFish() {
    echo &quot;Whale eat fish.\n&quot;;
  }
}
class Carp {
  public function __construct() {
    $this-&gt;name = &quot;Carp&quot;;
  }
  public function eatMoss() {
    echo &quot;Carp eat moss.\n&quot;;
  }
}
?&gt;
</code></pre><p>假设我们现在需要建一个动物馆，有eatFish()和eatMoss()接口，动物馆接口如下：</p>
<pre><code>&lt;?php
interface Animal {
  function eatFish();
  function eatMoss();
}
?&gt;
</code></pre><p>但是我们不能修改Whale和Carp类，这里就需要使用适配器了，创建两个适配器：</p>
<pre><code>&lt;?php
class WhaleAdapter extends Whale implements Animal {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
  }
  public function eatMoss() {
    echo &quot;Whale don&apos;t eat moss.\n&quot;;
  }
}
class CarpAdapter extends Carp implements Animal {
  public function __construct() {
    $this-&gt;name = &quot;Carp&quot;;
  }
  public function eatFish() {
    echo &quot;Carp don&apos;t eat moss.\n&quot;;
  }
}
?&gt;
</code></pre><p>然后是调用代码：</p>
<pre><code>&lt;?php
$whaleAdapter = new WhaleAdapter();
$whaleAdapter-&gt;eatFish();
$whaleAdapter-&gt;eatMoss();
$carpAdapter = new CarpAdapter();
$carpAdapter-&gt;eatMoss();
$carpAdapter-&gt;eatFish();
?&gt;
</code></pre><p>运行一下：</p>
<pre><code>Whale eat fish.
Whale don&apos;t eat moss.
Carp eat moss.
Carp don&apos;t eat moss.
</code></pre><h2 id="使用组件的适配器模式"><a href="#使用组件的适配器模式" class="headerlink" title="使用组件的适配器模式"></a>使用组件的适配器模式</h2><p>还是使用鲸鱼和鲤鱼的例子，不过这个时候适配器变成了：</p>
<pre><code>&lt;?php
class WhaleAdapter implements Animal {
  public function __construct() {
    $this-&gt;name = &quot;Whale&quot;;
    $this-&gt;whale = new Whale();
  }
  public function eatFish() {
    $this-&gt;whale-&gt;eatFish();
  }
  public function eatMoss() {
    echo &quot;Whale don&apos;t eat moss.\n&quot;;
  }
}
class CarpAdapter implements Animal {
  public function __construct() {
    $this-&gt;name = &quot;Carp&quot;;
    $this-&gt;carp = new Carp();
  }
  public function eatFish() {
    echo &quot;Carp don&apos;t eat moss.\n&quot;;
  }
  public function eatMoss() {
    $this-&gt;carp-&gt;eatMoss();
  }
}
?&gt;
</code></pre><p>其他的地方和使用继承的适配器模式一样，这里不再赘述。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>适配器模式在不修改现有代码的基础上，保留了架构。使用继承的适配器和使用组件的适配器各有利弊，继承的类冗余度/空间复杂度偏高，组件的调用栈/时间复杂度偏高，应该结合实际情况选择。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;在[PHP设计模式(七)：设计模式分类][2]中我们提到过结构设计模式(Structural&lt;br&gt;patterns)，结构设计模式专注于设
    
    </summary>
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/categories/php/"/>
    
    
      <category term="php" scheme="http://ronghuaxueleng.github.io/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>从 JavaScript 数组去重谈性能优化</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/javascript/%E4%BB%8E%20JavaScript%20%E6%95%B0%E7%BB%84%E5%8E%BB%E9%87%8D%E8%B0%88%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/javascript/从 JavaScript 数组去重谈性能优化/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T08:34:52.608Z</updated>
    
    <content type="html"><![CDATA[<h2 id="缘由"><a href="#缘由" class="headerlink" title="缘由"></a>缘由</h2><p>JavaScript 数组去重经常出现在前端招聘的笔试题里，比如：</p>
<blockquote>
<p>有数组 <code>var arr = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;1&#39;, 0, &#39;c&#39;, 1, &#39;&#39;, 1, 0]</code>，请用 JavaScript<br>实现去重函数 <code>unqiue</code>，使得 <code>unique(arr)</code> 返回 <code>[&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;1&#39;, 0, 1, &#39;&#39;]</code></p>
</blockquote>
<p>作为笔试题，考点有二：</p>
<ol>
<li><p><strong>正确</strong>。别小看这个考点，考虑到 JavaScript 经常要在浏览器上运行，在千姿百态的各种浏览器环境下要保障一个函数的正确性可不是一件简单的事，不信你继续读完这篇博客。</p>
</li>
<li><p><strong>性能</strong>。虽然大部分情况下 JavaScript 语言本身（狭义范畴，不包含 DOM 等延拓）不会导致性能问题，但很不幸这是一道考题，因此面试官们还是会把性能作为一个考点。</p>
</li>
</ol>
<p>在继续往下阅读之前，建议先实现一个自己认为最好的版本。</p>
<h2 id="直觉方案"><a href="#直觉方案" class="headerlink" title="直觉方案"></a>直觉方案</h2><p>对于数组去重，只要写过程序的，立刻就能得到第一个解法：</p>
<pre><code>function unique(arr) {
  var ret = []

  for (var i = 0; i &lt; arr.length; i++) {
    var item = arr[i]
    if (ret.indexOf(item) === -1) {
      ret.push(item)
    }
  }

  return ret
}
</code></pre><p>直觉往往很靠谱，在现代浏览器下，上面这个函数很正确，性能也不错。但前端最大的悲哀也是挑战之处在于，要支持各种运行环境。在 IE6-8 下，数组的<br><code>indexOf</code> 方法还不存在。直觉方案要稍微改造一下：</p>
<pre><code>var indexOf = [].indexOf ?
    function(arr, item) {
      return arr.indexOf(item)
    } :
    function indexOf(arr, item) {
      for (var i = 0; i &lt; arr.length; i++) {
        if (arr[i] === item) {
          return i
        }
      }
      return -1
    }

function unique(arr) {
  var ret = []

  for (var i = 0; i &lt; arr.length; i++) {
    var item = arr[i]
    if (indexOf(ret, item) === -1) {
      ret.push(item)
    }
  }

  return ret
}
</code></pre><p>写到这一步，正确性已没问题，但性能上，两重循环会让面试官们看了不爽。</p>
<h2 id="优化方案"><a href="#优化方案" class="headerlink" title="优化方案"></a>优化方案</h2><p>一谈到优化，往往就是八仙过海、百花齐放。但八仙往往不接地气，百花则很容易招来臭虫。数组去重的各种优化方案在此不一一讨论，下面只说最常用效果也很不错的一种。</p>
<pre><code>function unique(arr) {
  var ret = []
  var hash = {}

  for (var i = 0; i &lt; arr.length; i++) {
    var item = arr[i]
    var key = typeof(item) + item
    if (hash[key] !== 1) {
      ret.push(item)
      hash[key] = 1
    }
  }

  return ret
}
</code></pre><p>核心是构建了一个 <code>hash</code> 对象来替代 <code>indexOf</code>. 注意在 JavaScript 里，对象的键值只能是字符串，因此需要 <code>var key =
typeof(item) + item</code> 来区分数值 <code>1</code> 和字符串 <code>&#39;1&#39;</code> 等情况。</p>
<p>但优化真的很容易带来坑，比如上面的实现，对下面这种输入就无法判断：</p>
<pre><code>unique([ new String(1), new Number(1) ])
</code></pre><p>可以继续修改代码，做到性能和正确性都很好。但往往，这带来的结果并不好。</p>
<h2 id="真实需求"><a href="#真实需求" class="headerlink" title="真实需求"></a>真实需求</h2><p>写到这里，这篇博客才算进入正题。程序员心中都会有一些梦想，比如写出又通用性能又好的普适函数。这种梦想是让程序员变得卓越的重要内驱力，但倘若不加以控制，也很容<br>易走入迷途。</p>
<p>回到性能优化。这年头有各种各样优化，核心系统、数据库、网络、前端等等，所有这些优化，都必须回答下面这个问题：</p>
<ol>
<li><p><strong>当前有什么</strong>。在什么场景下进行优化，场景下有哪些具体限制。理清限制很重要，限制往往带来自由。</p>
</li>
<li><p><strong>究竟要什么</strong>。优化的目的是什么。是提高稳定性，还是增大吞吐量，抑或减少用户等待时间。在回答这个问题之前，优化都是徒劳。对这个问题的准确回答，能为优化带来具体可测量的参数，这样优化才有目标。</p>
</li>
<li><p><strong>可以放弃什么</strong>。鱼与熊掌不可兼得。优化的本质是在具体场景下的取舍、权衡。什么都不愿意放弃的话，优化往往会举步维艰。</p>
</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;缘由&quot;&gt;&lt;a href=&quot;#缘由&quot; class=&quot;headerlink&quot; title=&quot;缘由&quot;&gt;&lt;/a&gt;缘由&lt;/h2&gt;&lt;p&gt;JavaScript 数组去重经常出现在前端招聘的笔试题里，比如：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;有数组 &lt;code&gt;var ar
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>44个 Javascript 变态题解析 (上)</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/13/javascript/44%E4%B8%AA%20Javascript%20%E5%8F%98%E6%80%81%E9%A2%98%E8%A7%A3%E6%9E%90%20(%E4%B8%8A)/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/13/javascript/44个 Javascript 变态题解析 (上)/</id>
    <published>2016-10-12T16:00:00.000Z</published>
    <updated>2016-10-13T09:07:50.645Z</updated>
    
    <content type="html"><![CDATA[<p>原题来自: <a href="http://javascript-puzzlers.herokuapp.com/" target="_blank" rel="external">javascript-puzzlers</a></p>
<p>读者可以先去做一下感受感受. 当初笔者的成绩是 21/44…</p>
<p>当初笔者做这套题的时候不仅怀疑智商, 连人生都开始怀疑了….</p>
<p>不过, 对于基础知识的理解是深入编程的前提. 让我们一起来看看这些变态题到底变态不变态吧!</p>
<h2 id="第1题"><a href="#第1题" class="headerlink" title="第1题"></a>第1题</h2><pre><code>[&quot;1&quot;, &quot;2&quot;, &quot;3&quot;].map(parseInt)
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map" target="_blank" rel="external">Array/map</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/parseInt" target="_blank" rel="external">Number/parseInt</a></li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/parseInt" target="_blank" rel="external">Global_Objects/parseInt</a></li>
<li><a href="http://www.w3school.com.cn/jsref/jsref_parseInt.asp" target="_blank" rel="external">JavaScript parseInt</a></li>
</ul>
<p>首先, map接受两个参数, 一个回调函数 callback, 一个回调函数的this值</p>
<p>其中回调函数接受三个参数 currentValue, index, arrary;</p>
<p>而题目中, map只传入了回调函数–parseInt.</p>
<p>其次, parseInt 只接受两个两个参数 string, radix(基数).</p>
<blockquote>
<p>在没有指定基数，或者基数为 0 的情况下，JavaScript 作如下处理：</p>
</blockquote>
<p>&gt;</p>
<blockquote>
<ul>
<li><p>如果字符串 string 以”0x”或者”0X”开头, 则基数是16 (16进制).</p>
</li>
<li><p>如果字符串 string 以”0”开头, 基数是8（八进制）或者10（十进制），那么具体是哪个基数由实现环境决- 定。ECMAScript 5<br>规定使用10，但是并不是所有的浏览器都遵循这个规定。因此，永远都要明确给出radix参数的值。</p>
</li>
<li><p>如果字符串 string 以其它任何值开头，则基数是10 (十进制)。</p>
</li>
</ul>
</blockquote>
<p>所以本题即问</p>
<pre><code>parseInt(&apos;1&apos;, 0);
parseInt(&apos;2&apos;, 1);
parseInt(&apos;3&apos;, 2);
</code></pre><p>首先后两者参数不合法.</p>
<p>所以答案是 <code>[1, NaN, NaN]</code></p>
<h2 id="第2题"><a href="#第2题" class="headerlink" title="第2题"></a>第2题</h2><pre><code>[typeof null, null instanceof Object]
</code></pre><p>两个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof" target="_blank" rel="external">Operators/typeof</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/instanceof" target="_blank" rel="external">Operators/instanceof</a></li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/instanceof" target="_blank" rel="external">Operators/instanceof(中)</a></li>
</ul>
<p>typeof 返回一个表示类型的字符串.</p>
<p>instanceof 运算符用来检测 constructor.prototype 是否存在于参数 object 的原型链上.</p>
<p>这个题可以直接看链接… 因为 <code>typeof null === &#39;object&#39;</code> 自语言之初就是这样….</p>
<p>typeof 的结果请看下表:</p>
<pre><code>type         result
Undefined   &quot;undefined&quot;
Null        &quot;object&quot;
Boolean     &quot;boolean&quot;
Number      &quot;number&quot;
String      &quot;string&quot;
Symbol      &quot;symbol&quot;
Host object Implementation-dependent
Function    &quot;function&quot;
Object      &quot;object&quot;
</code></pre><p>所以答案 <code>[object, false]</code></p>
<h2 id="第3题"><a href="#第3题" class="headerlink" title="第3题"></a>第3题</h2><pre><code>[ [3,2,1].reduce(Math.pow), [].reduce(Math.pow) ]
</code></pre><p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce" target="_blank" rel="external">Array/Reduce</a></li>
</ul>
<p><code>arr.reduce(callback[, initialValue])</code></p>
<p>reduce接受两个参数, 一个回调, 一个初始值.</p>
<p>回调函数接受四个参数 <code>previousValue, currentValue, currentIndex, array</code></p>
<p>需要注意的是 <code>If the array is empty and no initialValue was provided, TypeError
would be thrown.</code></p>
<p>所以第二个表达式会报异常. 第一个表达式等价于 <code>Math.pow(3, 2) =&gt; 9; Math.pow(9, 1) =&gt;9</code></p>
<p>答案 <code>an error</code></p>
<h2 id="第4题"><a href="#第4题" class="headerlink" title="第4题"></a>第4题</h2><pre><code>var val = &apos;smtg&apos;;
console.log(&apos;Value is &apos; + (val === &apos;smtg&apos;) ? &apos;Something&apos; : &apos;Nothing&apos;);
</code></pre><p>两个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence" target="_blank" rel="external">Operators/Operator_Precedence</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Conditional_Operator" target="_blank" rel="external">Operators/Conditional_Operator</a></li>
</ul>
<p>简而言之 <code>+</code> 的优先级 大于 <code>?</code></p>
<p>所以原题等价于 <code>&#39;Value is true&#39; ? &#39;Somthing&#39; : &#39;Nonthing&#39;</code> 而不是 <code>&#39;Value is&#39; + (true ?
&#39;Something&#39; : &#39;Nonthing&#39;)</code></p>
<p>答案 <code>&#39;Something&#39;</code></p>
<h2 id="第5题"><a href="#第5题" class="headerlink" title="第5题"></a>第5题</h2><pre><code>var name = &apos;World!&apos;;
(function () {
    if (typeof name === &apos;undefined&apos;) {
        var name = &apos;Jack&apos;;
        console.log(&apos;Goodbye &apos; + name);
    } else {
        console.log(&apos;Hello &apos; + name);
    }
})();
</code></pre><p>这个相对简单, 一个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Glossary/Hoisting" target="_blank" rel="external">Hoisting</a></li>
</ul>
<p>在 JavaScript中， functions 和 variables 会被提升。变量提升是JavaScript将声明移至作用域 scope<br>(全局域或者当前函数作用域) 顶部的行为。</p>
<p>这个题目相当于</p>
<pre><code>var name = &apos;World!&apos;;
(function () {
    var name;
    if (typeof name === &apos;undefined&apos;) {
        name = &apos;Jack&apos;;
        console.log(&apos;Goodbye &apos; + name);
    } else {
        console.log(&apos;Hello &apos; + name);
    }
})();
</code></pre><p>所以答案是 <code>&#39;Goodbye Jack&#39;</code></p>
<h2 id="第6题"><a href="#第6题" class="headerlink" title="第6题"></a>第6题</h2><pre><code>var END = Math.pow(2, 53);
var START = END - 100;
var count = 0;
for (var i = START; i &lt;= END; i++) {
    count++;
}
console.log(count);
</code></pre><p>一个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Infinity" target="_blank" rel="external">Infinity</a><br><del>在 JS 里, Math.pow(2, 53) == 9007199254740992 是可以表示的最大值. 最大值加一还是最大值.<br>所以循环不会停.</del></li>
</ul>
<p>补充: <a href="https://github.com/jelly7723" target="_blank" rel="external">@jelly7723</a></p>
<blockquote>
<p>js中可以表示的最大整数不是2的53次方，而是1.7976931348623157e+308。<br>2的53次方不是js能表示的最大整数而应该是能正确计算且不失精度的最大整数，可以参见js权威指南。<br>9007199254740992 +1还是 9007199254740992 ，这就是因为精度问题，如果 9007199254740992 +11或者<br>9007199254740992 +111的话，值是会发生改变的，只是这时候计算的结果不是正确的值，就是因为精度丢失的问题。</p>
</blockquote>
<h2 id="第7题"><a href="#第7题" class="headerlink" title="第7题"></a>第7题</h2><pre><code>var ary = [0,1,2];
ary[10] = 10;
ary.filter(function(x) { return x === undefined;});
</code></pre><p>答案是 <code>[]</code></p>
<p>看一篇文章理解稀疏数组</p>
<ul>
<li><a href="http://www.cnblogs.com/ziyunfei/archive/2012/09/16/2687165.html" target="_blank" rel="external">译 JavaScript中的稀疏数组与密集数组</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/filter" target="_blank" rel="external">Array/filter</a></li>
</ul>
<p>我们来看一下 Array.prototype.filter 的 polyfill:</p>
<pre><code>if (!Array.prototype.filter) {
  Array.prototype.filter = function(fun/*, thisArg*/) {
    &apos;use strict&apos;;

    if (this === void 0 || this === null) {
      throw new TypeError();
    }

    var t = Object(this);
    var len = t.length &gt;&gt;&gt; 0;
    if (typeof fun !== &apos;function&apos;) {
      throw new TypeError();
    }

    var res = [];
    var thisArg = arguments.length &gt;= 2 ? arguments[1] : void 0;
    for (var i = 0; i &lt; len; i++) {
      if (i in t) { // 注意这里!!!
        var val = t[i];
        if (fun.call(thisArg, val, i, t)) {
          res.push(val);
        }
      }
    }

    return res;
  };
}
</code></pre><p>我们看到在迭代这个数组的时候, 首先检查了这个索引值是不是数组的一个属性, 那么我们测试一下.</p>
<pre><code>0 in ary; =&gt; true
3 in ary; =&gt; false
10 in ary; =&gt; true
</code></pre><p>也就是说 从 3 - 9 都是没有初始化的’坑’!, 这些索引并不存在与数组中. 在 array 的函数调用的时候是会跳过这些’坑’的.</p>
<h2 id="第8题"><a href="#第8题" class="headerlink" title="第8题"></a>第8题</h2><pre><code>var two   = 0.2
var one   = 0.1
var eight = 0.8
var six   = 0.6
[two - one == one, eight - six == two]
</code></pre><ul>
<li><a href="http://ourjs.com/detail/54695381bc3f9b154e000046" target="_blank" rel="external">JavaScript的设计缺陷?浮点运算：0.1 + 0.2 != 0.3</a></li>
</ul>
<p>IEEE 754标准中的浮点数并不能精确地表达小数</p>
<p>那什么时候精准, 什么时候不经准呢? 笔者也不知道…</p>
<p>答案 <code>[true, false]</code></p>
<h2 id="第9题"><a href="#第9题" class="headerlink" title="第9题"></a>第9题</h2><pre><code>function showCase(value) {
    switch(value) {
    case &apos;A&apos;:
        console.log(&apos;Case A&apos;);
        break;
    case &apos;B&apos;:
        console.log(&apos;Case B&apos;);
        break;
    case undefined:
        console.log(&apos;undefined&apos;);
        break;
    default:
        console.log(&apos;Do not know!&apos;);
    }
}
showCase(new String(&apos;A&apos;));
</code></pre><p>两个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/switch" target="_blank" rel="external">Statements/switch</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String" target="_blank" rel="external">String</a></li>
</ul>
<p>switch 是严格比较, String 实例和 字符串不一样.</p>
<pre><code>var s_prim = &apos;foo&apos;;
var s_obj = new String(s_prim);

console.log(typeof s_prim); // &quot;string&quot;
console.log(typeof s_obj);  // &quot;object&quot;
console.log(s_prim === s_obj); // false
</code></pre><p>答案是 <code>&#39;Do not know!&#39;</code></p>
<h2 id="第10题"><a href="#第10题" class="headerlink" title="第10题"></a>第10题</h2><pre><code>function showCase2(value) {
    switch(value) {
    case &apos;A&apos;:
        console.log(&apos;Case A&apos;);
        break;
    case &apos;B&apos;:
        console.log(&apos;Case B&apos;);
        break;
    case undefined:
        console.log(&apos;undefined&apos;);
        break;
    default:
        console.log(&apos;Do not know!&apos;);
    }
}
showCase2(String(&apos;A&apos;));
</code></pre><p>解释:<br><code>String(x) does not create an object but does return a string, i.e. typeof
String(1) === &quot;string&quot;</code></p>
<p>还是刚才的知识点, 只不过 String 不仅是个构造函数 直接调用返回一个字符串哦.</p>
<p>答案 <code>&#39;Case A&#39;</code></p>
<h2 id="第11题"><a href="#第11题" class="headerlink" title="第11题"></a>第11题</h2><pre><code>function isOdd(num) {
    return num % 2 == 1;
}
function isEven(num) {
    return num % 2 == 0;
}
function isSane(num) {
    return isEven(num) || isOdd(num);
}
var values = [7, 4, &apos;13&apos;, -9, Infinity];
values.map(isSane);
</code></pre><p>一个知识点</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Arithmetic_Operators#Remainder" target="_blank" rel="external">Arithmetic_Operators#Remainder</a></li>
</ul>
<p>此题等价于</p>
<pre><code>7 % 2 =&gt; 1
4 % 2 =&gt; 0
&apos;13&apos; % 2 =&gt; 1
-9 % % 2 =&gt; -1
Infinity % 2 =&gt; NaN
</code></pre><p>需要注意的是 余数的正负号随第一个操作数.</p>
<p>答案 <code>[true, true, true, false, false]</code></p>
<h2 id="第12题"><a href="#第12题" class="headerlink" title="第12题"></a>第12题</h2><pre><code>parseInt(3, 8)
parseInt(3, 2)
parseInt(3, 0)
</code></pre><p>第一个题讲过了, 答案 <code>3, NaN, 3</code></p>
<h2 id="第13题"><a href="#第13题" class="headerlink" title="第13题"></a>第13题</h2><pre><code>Array.isArray( Array.prototype )
</code></pre><p>一个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/prototype" target="_blank" rel="external">Array/prototype</a></li>
</ul>
<p>一个鲜为人知的实事: <code>Array.prototype =&gt; []</code>;</p>
<p>答案: <code>true</code></p>
<h2 id="第14题"><a href="#第14题" class="headerlink" title="第14题"></a>第14题</h2><pre><code>var a = [0];
if ([0]) {
  console.log(a == true);
} else {
  console.log(&quot;wut&quot;);
}
</code></pre><ul>
<li><a href="https://dorey.github.io/JavaScript-Equality-Table/" target="_blank" rel="external">JavaScript-Equality-Table</a></li>
<li><em>更新</em><a href="https://zhuanlan.zhihu.com/p/21650547" target="_blank" rel="external">通过一张简单的图，让你彻底地、永久地搞懂JS的==运算</a> 非常不错的一篇文章!</li>
</ul>
<p>解析:</p>
<ul>
<li><code>Boolean([0]) === true</code></li>
<li>[0] == true <ul>
<li>true 转换为数字 =&gt; 1</li>
<li>[0] 转化为数字失败, 转化为字符串 ‘0’, 转化成数字 =&gt; 0</li>
<li>0 !== 1</li>
</ul>
</li>
</ul>
<p>答案: <code>false</code></p>
<h2 id="第15题"><a href="#第15题" class="headerlink" title="第15题"></a>第15题</h2><pre><code>[]==[]
</code></pre><p>[] 是Object, 两个 Object 不相等</p>
<p>答案是 <code>false</code></p>
<h2 id="第16题"><a href="#第16题" class="headerlink" title="第16题"></a>第16题</h2><pre><code>&apos;5&apos; + 3
&apos;5&apos; - 3
</code></pre><p>两个知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Arithmetic_Operators#Addition" target="_blank" rel="external">Arithmetic_Operators#Addition</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Arithmetic_Operators#Subtraction" target="_blank" rel="external">Arithmetic_Operators#Subtraction</a></li>
</ul>
<p><code>+</code> 用来表示两个数的和或者字符串拼接, <code>-</code>表示两数之差.</p>
<p>请看例子, 体会区别:</p>
<pre><code>&gt; &apos;5&apos; + 3
&apos;53&apos;
&gt; 5 + &apos;3&apos;
&apos;53&apos;
&gt; 5 - &apos;3&apos;
2
&gt; &apos;5&apos; - 3
2
&gt; &apos;5&apos; - &apos;3&apos;
2
</code></pre><p>也就是说 <code>-</code> 会尽可能的将两个操作数变成数字, 而 <code>+</code> 如果两边不都是数字, 那么就是字符串拼接.</p>
<p>答案是 <code>&#39;53&#39;, 2</code></p>
<h2 id="第17题"><a href="#第17题" class="headerlink" title="第17题"></a>第17题</h2><pre><code>1 + - + + + - + 1
</code></pre><p>这里应该是(倒着看)</p>
<pre><code>1 + (a)  =&gt; 2
a = - (b) =&gt; 1
b = + (c) =&gt; -1
c = + (d) =&gt; -1
d = + (e) =&gt; -1
e = + (f) =&gt; -1
f = - (g) =&gt; -1
g = + 1   =&gt; 1
</code></pre><p>所以答案 <code>2</code></p>
<h2 id="第18题"><a href="#第18题" class="headerlink" title="第18题"></a>第18题</h2><pre><code>var ary = Array(3);
ary[0]=2
ary.map(function(elem) { return &apos;1&apos;; });
</code></pre><p>稀疏数组. 同第7题.</p>
<p>题目中的数组其实是一个长度为3, 但是没有内容的数组, array 上的操作会跳过这些未初始化的’坑’.</p>
<p>所以答案是 <code>[&quot;1&quot;, undefined × 2]</code></p>
<p>这里贴上 Array.prototype.map 的 polyfill.</p>
<pre><code>Array.prototype.map = function(callback, thisArg) {

        var T, A, k;

        if (this == null) {
            throw new TypeError(&apos; this is null or not defined&apos;);
        }

        var O = Object(this);
        var len = O.length &gt;&gt;&gt; 0;
        if (typeof callback !== &apos;function&apos;) {
            throw new TypeError(callback + &apos; is not a function&apos;);
        }
        if (arguments.length &gt; 1) {
            T = thisArg;
        }
        A = new Array(len);
        k = 0;
        while (k &lt; len) {
            var kValue, mappedValue;
            if (k in O) {
                kValue = O[k];
                mappedValue = callback.call(T, kValue, k, O);
                A[k] = mappedValue;
            }
            k++;
        }
        return A;
    };
</code></pre><h2 id="第19题"><a href="#第19题" class="headerlink" title="第19题"></a>第19题</h2><pre><code>function sidEffecting(ary) {
  ary[0] = ary[2];
}
function bar(a,b,c) {
  c = 10
  sidEffecting(arguments);
  return a + b + c;
}
bar(1,1,1)
</code></pre><p>这是一个大坑, 尤其是涉及到 ES6语法的时候</p>
<p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/arguments" target="_blank" rel="external">Functions/arguments</a></li>
</ul>
<p>首先 <code>The arguments object is an Array-like object corresponding to the
arguments passed to a function.</code></p>
<p>也就是说 <code>arguments</code> 是一个 <code>object</code>, c 就是 arguments<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map" target="_blank" rel="external">2</a>, 所以对于 c 的修改就是对 arguments<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map" target="_blank" rel="external">2</a><br>的修改.</p>
<p>所以答案是 <code>21</code>.</p>
<p>然而!!!!!!</p>
<p>当函数参数涉及到 <code>any rest parameters, any default parameters or any destructured
parameters</code> 的时候, 这个 arguments 就不在是一个 <code>mapped arguments object</code> 了…..</p>
<p>请看:</p>
<pre><code>function sidEffecting(ary) {
  ary[0] = ary[2];
}
function bar(a,b,c=3) {
  c = 10
  sidEffecting(arguments);
  return a + b + c;
}
bar(1,1,1)
</code></pre><p>答案是 <code>12</code> !!!!</p>
<p>请读者细细体会!!</p>
<h2 id="第20题"><a href="#第20题" class="headerlink" title="第20题"></a>第20题</h2><pre><code>var a = 111111111111111110000,
    b = 1111;
a + b;
</code></pre><p>答案还是 <code>111111111111111110000</code>. 解释是 <code>Lack of precision for numbers in JavaScript
affects both small and big numbers.</code> 但是笔者不是很明白……………. 请读者赐教!</p>
<h2 id="第21题"><a href="#第21题" class="headerlink" title="第21题"></a>第21题</h2><pre><code>var x = [].reverse;
x();
</code></pre><p>这个题有意思!</p>
<p>知识点:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reverse" target="_blank" rel="external">Array/reverse</a></li>
</ul>
<p><code>The reverse method transposes the elements of the calling array object in
place, mutating the array, and returning a reference to the array.</code></p>
<p>也就是说 最后会返回这个调用者(this), 可是 x 执行的时候是上下文是全局. 那么最后返回的是 <code>window</code>.</p>
<p>补充:</p>
<blockquote>
<p><a href="https://github.com/stellar91" target="_blank" rel="external">@stellar91</a> 这个笔者实践了一下 发现 firefox 是 window, chrome 报错 <code>VM190:2 Uncaught
TypeError: Array.prototype.reverse called on null or undefined(…)</code> 可能是实现不同, 在<br>chrome 中应该是对调用者做了检查.</p>
</blockquote>
<p>答案是 <code>window</code></p>
<h2 id="第22题"><a href="#第22题" class="headerlink" title="第22题"></a>第22题</h2><pre><code>Number.MIN_VALUE &gt; 0
</code></pre><p><code>true</code></p>
<p>#</p>
<p>今天先到这里, 下次我们来看后22个题!</p>
<p><a href="https://github.com/xiaoyu2er/blog/issues/3" target="_blank" rel="external">44个 Javascript 变态题解析 (下)</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;原题来自: &lt;a href=&quot;http://javascript-puzzlers.herokuapp.com/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;javascript-puzzlers&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;读者可以先去做一下感受感受. 当初
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>设计原则和编程技巧</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/12/JavaScript%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%8E%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/20%E8%AE%BE%E8%AE%A1%E5%8E%9F%E5%88%99%E5%92%8C%E7%BC%96%E7%A8%8B%E6%8A%80%E5%B7%A7/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/12/JavaScript设计模式与开发实践/20设计原则和编程技巧/</id>
    <published>2016-10-11T16:00:00.000Z</published>
    <updated>2016-10-12T09:43:08.156Z</updated>
    
    <content type="html"><![CDATA[<hr>
<h3 id="单一职责原则"><a href="#单一职责原则" class="headerlink" title="单一职责原则"></a>单一职责原则</h3><p>就一个类而言，应该仅有一个引起它变化的原因。在JavaScript中，需要用到类的场景并不太多，单一职责原则（SRP）更多的是被运用在对象或者方法级别上。<br>总之，SRP原则体现为：一个对象（方法）只做一件事情。<br>例如：</p>
<ul>
<li><p>代理模式</p>
</li>
<li><p>迭代器模式</p>
</li>
<li><p>单例模式</p>
</li>
<li><p>装饰者模式<br>但是，并不是所有的职责都应该一一分离的，一方面如果随着需求的变化，有两个职责是同时变化的，那就不必分离他们。比如在ajax请求的时候，创建xhr对象和发送xhr请求几乎总是在一起的，那么创建xhr对象的职责和发送xhr对象的职责就没有必要分开。</p>
</li>
</ul>
<p>另一方面，职责的变化轴线仅当它们确定会发生变化时才有意义，即使两个职责已经被耦合在一起但他们没有发生改变的征兆，那么也许没有必要主动分离他们，在代码需要重构的时候再分离也不迟。</p>
<h3 id="最少知识原则"><a href="#最少知识原则" class="headerlink" title="最少知识原则"></a>最少知识原则</h3><p>最少知识原则（LKP）说的是一个软件实体应当尽可能少与其他实体发生相互作用。这里的软件的实体是一个广义的概念，不仅包括对象，还包括系统，类，模块，函数，变量等。<br>单一职责原则指导我们把对象划分成较小的粒度，这可以提高对象的可复用性。但越来越多的对象之间可能会产生错综复杂的联系，如果修改了其中一个对象，很可能会影响到跟它相互引用的其他对象。对象和对象耦合在一起，有可能会降低他们的复用性。在程序中，对象的“朋友”太多并不是一件好事。<br>最少知识原则要求我们在设计程序时，应当尽量减少对象之间的交互。如果两个对象之间不必彼此直接通信，那么两个对象就不要发生直接的相互联系。常见的做法是引入一个第三者对象，来承担这些对象之间的通信作用。如果一些对象需要向另一些对象发起请求，可以通过第三者对象来转发这些请求。<br>体现地方：</p>
<ul>
<li><p>中介者模式</p>
</li>
<li><p>外观模式</p>
</li>
</ul>
<h3 id="开放-封闭原则"><a href="#开放-封闭原则" class="headerlink" title="开放-封闭原则"></a>开放-封闭原则</h3><p>在面向对象的程序设计中，开放-封闭原则（OCP）是最重要的一条原则。他的定义为：软件实体（类，模块，函数）等应该是可以拓展的，但是不可修改的。<br>举例：</p>
<ul>
<li><p>发布-订阅模式</p>
</li>
<li><p>模板方法模式</p>
</li>
<li><p>策略模式</p>
</li>
<li><p>代理模式</p>
</li>
<li><p>职责链模式</p>
</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;h3 id=&quot;单一职责原则&quot;&gt;&lt;a href=&quot;#单一职责原则&quot; class=&quot;headerlink&quot; title=&quot;单一职责原则&quot;&gt;&lt;/a&gt;单一职责原则&lt;/h3&gt;&lt;p&gt;就一个类而言，应该仅有一个引起它变化的原因。在JavaScript中，需要用到类的场景并不太多，单
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>this关键字</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/12/JavaScript%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%8E%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/1this%E5%85%B3%E9%94%AE%E5%AD%97/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/12/JavaScript设计模式与开发实践/1this关键字/</id>
    <published>2016-10-11T16:00:00.000Z</published>
    <updated>2016-10-12T09:50:39.586Z</updated>
    
    <content type="html"><![CDATA[<hr>
<blockquote>
<p>this的指向大概可以分成四种：</p>
<ul>
<li>作为对象的方法调用</li>
<li>作为普通函数调用</li>
<li>构造器调用</li>
<li>Function.prototype.call或Function.prototype.apply调用</li>
</ul>
</blockquote>
<h3 id="1-作为对象的时候this指向该对象"><a href="#1-作为对象的时候，this指向该对象" class="headerlink" title="1. 作为对象的时候，this指向该对象"></a>1. 作为对象的时候，<strong>this指向该对象</strong></h3><pre><code>var obj = {
    a: 1,
    getA: function() {
        alert(this === obj); //true;
        alert(this.a); //1
    }
};
obj.getA();
</code></pre><h3 id="2-作为普通函数调用this总是指向全局对象在浏览器中就是window"><a href="#2-作为普通函数调用，this总是指向全局对象（在浏览器中就是window）" class="headerlink" title="2. 作为普通函数调用，this总是指向全局对象（在浏览器中就是window）"></a>2. 作为普通函数调用，<strong>this总是指向全局对象（在浏览器中就是window）</strong></h3><pre><code>window.name = &apos;globalName&apos;;
var getName = function() {
    return this.name;
};
console.log(getName()); //globalName
</code></pre><p>再例如局部的callback，它作为普通函数调用时，callback内部的this指向了windows对象，解决方法则可以用一个变量保存this，例如</p>
<pre><code>document.getElemmentById(&apos;div1&apos;).onclick = function() {
    var that = this;
    var callback = function() {
        alert(that.id); //div1;
    }
    callback();
}
</code></pre><h3 id="3-构造器调用"><a href="#3-构造器调用" class="headerlink" title="3. 构造器调用"></a>3. 构造器调用</h3><p>当用<code>new</code>运算符调用函数时，该函数总会返回一个对象，通常情况this就是指向返回的这个对象，但如果构造器显示返回了一个object类的对象，那运算结果最终返回的是那个对象而不是this，例如</p>
<pre><code>var MyClass1 = function() {
    this.name = &apos;111&apos;;
}
var obj1 = new MyClass1();
alert(obj1.name); //111

var MyClass2 = function() {
    this.name = &apos;111&apos;;
    return { //显式的返回一个对象
        name: &apos;222&apos;;
    }
}
var obj2 = new MyClass2();
alert(obj2.name); //222
</code></pre><h3 id="4-functionprototypecall或functionprototypeapply可以动态的改变传入函数的this"><a href="#4-Function-prototype-call或Function-prototype-apply可以动态的改变传入函数的this" class="headerlink" title="4. Function.prototype.call或Function.prototype.apply可以动态的改变传入函数的this"></a>4. Function.prototype.call或Function.prototype.apply可以动态的改变传入函数的this</h3><ul>
<li><p>两者区别：传入参数不同，apply接受两个参数，第一个为this指向，第二个为一个带下标的集合，这个集合可以为数组也可以为类数组；call传入的参数数量不固定，第一个也是代表this指向，第二个开始往后每个参数被依次传入函数。</p>
</li>
<li><p>改变this例子</p>
</li>
</ul>
<pre><code>var obj1 = {
    name: &apos;111&apos;;
};
var obj2 = {
    name: &apos;222&apos;;
};
window.name = &apos;window&apos;;
var getNmae = function() {
    alert(this.name);
}
getName(); //window
getName.call(obj1); //111
getName.call(obj2); //222
</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;blockquote&gt;
&lt;p&gt;this的指向大概可以分成四种：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;作为对象的方法调用&lt;/li&gt;
&lt;li&gt;作为普通函数调用&lt;/li&gt;
&lt;li&gt;构造器调用&lt;/li&gt;
&lt;li&gt;Function.prototype.call或Function.prot
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>装饰者模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/12/JavaScript%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%8E%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/17%E8%A3%85%E9%A5%B0%E8%80%85%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/12/JavaScript设计模式与开发实践/17装饰者模式/</id>
    <published>2016-10-11T16:00:00.000Z</published>
    <updated>2016-10-12T09:40:34.633Z</updated>
    
    <content type="html"><![CDATA[<hr>
<blockquote>
<p>装饰者模式：给对象动态地增加职责。跟继承相比，装饰者是一种更轻便灵活的做法，是一种“即用即付”的方式。</p>
</blockquote>
<h3 id="js的装饰者"><a href="#js的装饰者" class="headerlink" title="js的装饰者"></a>js的装饰者</h3><p>假设我们在编写一个飞机大战游戏，这个飞机可以升级，发射导弹，发射原子弹</p>
<pre><code>var plane = {
    fire: function() {
        console.log(&apos;发射普通子弹&apos;);
    }
};
var missileDecorator = function() {
    console.log(&apos;发射导弹&apos;);
};
var atomDecorator = function() {
    console.log(&apos;发射原子弹&apos;);
};
var fire1 = plane.fire;
plane.fire = function() {
    fire1();
    missileDecorator();
};

var fire2 = plane.fire;
plane.fire = function() {
    fire2();
    atomDecorator();
};
plane.fire(); //&quot;发射普通子弹&quot;&quot;发射导弹&quot;&quot;发射原子弹&quot;
</code></pre><h3 id="用aop装饰函数"><a href="#用AOP装饰函数" class="headerlink" title="用AOP装饰函数"></a>用AOP装饰函数</h3><p>首先给出<code>Function.prototype.before</code>方法和<code>Function.prototype.after</code>方法</p>
<pre><code>Function.prototype.before = function (beforefn) {
  var __self = this; //保存原函数的引用
  return function() {  //返回包含了原函数和新函数的代理函数
    beforefn.apply(this, arguments); //执行新函数，且保证this不被劫持，新函数接受的参数也会原封不动的传入原函数，新函数在原函数之前执行
    return __self.apply(this, arguments); //执行原函数并返回原函数的执行结果，并保证this不被劫持
  };
};
Function.prototype.after = function (afterfn) {
  var __self = this; 
  return function() {
    var ret = __self.apply(this, arguments); 
    afterfn.apply(this, arguments); 
    return ret;
  };
};
</code></pre><p>需求：<br>页面中有个按钮，点击会弹出登录浮层，与此同时要进行数据上报， 这时候我们最好去分离业务代码和数据统计代码，以便维护。</p>
<pre><code>var showLogin = function() {
    console.log(&apos;打开登陆浮层&apos;);
    log(this.getAttribute(&apos;tag&apos;));
}
var log = function(tag) {
    console.log(&apos;上报标签为:&apos; + tag);
}
document,getElementById(&apos;button&apos;).onclick = showLogin;
</code></pre><p>以上，<code>showLogin</code>函数既要负责打开登录浮层，又要负责数据上报，这是两个层面的功能，却被耦合在一个函数里，我们做点分离..</p>
<pre><code>var showLogin = function() {
    console.log(&apos;打开登陆浮层&apos;);
}
var log = function(tag) {
    console.log(&apos;上报标签为:&apos; + tag);
}

showLogin = showLogin.after(log); //打开登录浮层之后上报数据
document,getElementById(&apos;button&apos;).onclick = showLogin;
</code></pre><h3 id="装饰者模式和代理模式"><a href="#装饰者模式和代理模式" class="headerlink" title="装饰者模式和代理模式"></a>装饰者模式和代理模式</h3><p>代理模式和装饰者模式两种都描述了怎样为对象提供一定程度上的间接引用，他们的实现部分都保留了对另外一个对象的引用，并且向那个对象发送请求。<br>代理模式和装饰者模式最重要的区别在于他们的意图和设计目的。代理模式的目的是当直接访问本体不方便或者不符合需求时，为这个本体提供一个替代者。代理模式强调一种关系，这种关系一开始就以确定且通常只有一层代理-本体的引用。而装饰者模式的作用是为对象的动态加入行为，一开始不能确定对象的全部行为，通常装饰者模式会形成一条长长的链。</p>
]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;blockquote&gt;
&lt;p&gt;装饰者模式：给对象动态地增加职责。跟继承相比，装饰者是一种更轻便灵活的做法，是一种“即用即付”的方式。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;js的装饰者&quot;&gt;&lt;a href=&quot;#js的装饰者&quot; class=&quot;headerli
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>闭包</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/12/JavaScript%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%8E%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/2%E9%97%AD%E5%8C%85/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/12/JavaScript设计模式与开发实践/2闭包/</id>
    <published>2016-10-11T16:00:00.000Z</published>
    <updated>2016-10-12T06:26:36.705Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>闭包是js中一个难懂又必须征服的概念，他的形成与变量作用域以及变量的生存周期密切相关。</p>
</blockquote>
<h3 id="变量作用域和生存周期"><a href="#变量作用域和生存周期" class="headerlink" title="变量作用域和生存周期"></a>变量作用域和生存周期</h3><p>作用域，按字面理解，就是指变量的有效范围，超出这个范围就无法访问。<br>在函数中，里面函数可以访问外面的变量，但是外面无法访问内部变量。举个简单例子：</p>
<pre><code>var a = 1;
var fun1 = function() {
    var b = 2;
    var fun2 = function() {
        var c = 3;
        alert(b); //2
        alert(c); //3
    }
    fun2();
    alert(c); //c is not defined
};
fun1();
</code></pre><p>生存周期：全局变量的生存周期是永久的，除非我们主动销毁，但是普通对于用var声明的局部变量，当退出函数不再使用时，就会被销毁。但是当你使用到闭包时，情况就不一样了</p>
<pre><code>var fun = function() {
    var a = 1;
    return function() {
        a++;
        alert(a);
    }
};
var f = fun();
f(); //2
f(); //3
f(); //4
f(); //5
</code></pre><p>解释：当执行var f = fun();时，f返回了一个匿名函数的引用，它可以访问到fun()被调用的生产环境，而局部变量a一直在这个环境中。既然局部变量能<br>被外界访问，那它就有了不被销毁的理由，于是局部变量生命被延续了。</p>
<p>再例如：</p>
<pre><code>var nodes = document.getElemmentByTagName(&apos;div&apos;); //此处他的长度为5
for (var i = 0, len = nodes.length; i &lt; len; i++) {
    nodes[i].onclick = function() {
        alert(i);
    }
};
</code></pre><p>以上代码无论点哪个div显示的都是5（div节点的onclick是异步触发的，当事件触发时，循环已经结束，此时i的值是5）<br>解决方法：闭包大法好</p>
<pre><code>for (var i = 0, len = nodes.length; i &lt; len; i++) {
    (function(i) {
        nodes[i].onclick = function() {
            alert(i);
        }
    })(i)
};
</code></pre><p>通常，闭包可以有封装一些不需要暴露在全局的变量，延续局部变量寿命等等用途。</p>
<h3 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h3><p>命令模式的意图是把请求封装为对象，从而分离请求的发起者和请求的接收者（执行者）之间的耦合关系。在命令执行之前可预先往命令对象中植入命令的接收者。</p>
<pre><code>var TV = {
    open: function() {
        alert(&apos;open&apos;);
    },
    close: function() {
        alert(&apos;close&apos;);
    }
};

var createCommand = function(receiver) {
    var execute= function() {
        return receiver.open(); //执行命令打开电视
    }

    var undo= function() {
        return receiver.close(); //执行命令关闭电视
    }

    return {
        execute: execute,
        undo: undo
    }
};

var setComment = function(command) {
    document.getElementById(&apos;execute&apos;).onclick = function() {
        command.execute();
    }
    document.getElementById(&apos;undo&apos;).onclick = function() {
        command.undo();
    }
};
setCommand(createCommand(tv));
</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;闭包是js中一个难懂又必须征服的概念，他的形成与变量作用域以及变量的生存周期密切相关。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;变量作用域和生存周期&quot;&gt;&lt;a href=&quot;#变量作用域和生存周期&quot; class=&quot;headerlink&quot; t
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>策略模式</title>
    <link href="http://ronghuaxueleng.github.io/2016/10/12/JavaScript%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%8E%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/5%E7%AD%96%E7%95%A5%E6%A8%A1%E5%BC%8F/"/>
    <id>http://ronghuaxueleng.github.io/2016/10/12/JavaScript设计模式与开发实践/5策略模式/</id>
    <published>2016-10-11T16:00:00.000Z</published>
    <updated>2016-10-12T07:04:46.706Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>定义：定义一系列算法，把他们一个个封装起来，并且使他们可以相互替换。</p>
</blockquote>
<p>需求：绩效为S的人年终奖有4倍工资，绩效为A的人年终奖有3倍工资，绩效为B的人年终奖有2倍工资。</p>
<pre><code>var calculateBonus = function(performanceLevel, salary) {
    if (performanceLevel === &apos;S&apos;) {
        return salary * 4;
    }
    if (performanceLevel === &apos;A&apos;) {
        return salary * 3;
    }
    if (performanceLevel === &apos;B&apos;) {
        return salary * 2;
    }
};

calculateBonus(&apos;B&apos;, 20000); //輸出：40000
</code></pre><p>然而，calculateBonus函数比较庞大，包含了很多if语句，且缺乏弹性，如果增加新的绩效等级，复用性差，如果在程序的其他地方需要重用这些计算奖金的算法，我们的选择只有复制和粘贴。因此，我们要重构这段代码。</p>
<pre><code>var performanceS = function(salary) {
    return salary * 4;
}
var performanceA = function(salary) {
    return salary * 3;
}
var performanceB = function(salary) {
    return salary * 2;
}

var calculateBonus = function(performanceLevel, salary) {
    if (performanceLevel == &apos;S&apos;) {
        return performanceS(salary);
    }
    if (performanceLevel == &apos;A&apos;) {
        return performanceA(salary);
    }
    if (performanceLevel == &apos;B&apos;) {
        return performanceB(salary);
    }
};

calculateBonus(&apos;A&apos;, 10000);//輸出：30000
</code></pre><p>我们的程序得到了一定的改善，但calculateBonus函数可能越来越庞大，依旧缺乏弹性。</p>
<h3 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h3><p>这时候策略模式该出场了。</p>
<p><strong>我们要将算法的使用与算法的实现分离开来</strong></p>
<pre><code>//我们先把每种绩效的计算规则封装在对应的策略类里
var porformanceS = function() {};
porformanceS.prototype.calculate = function(salary) {
    return salary * 4;
};
var porformanceA = function() {};
porformanceA.prototype.calculate = function(salary) {
    return salary * 3;
};
var porformanceB = function() {};
porformanceB.prototype.calculate = function(salary) {
    return salary * 2;
};
//接下来定义奖金类Bonus:
var Bonus = function() {
    this.salary = null;
    this.strategy = null;
};

Bonus.prototype.setSalary = function(salary) {
    this.salary = salary;
}
Bonus.prototype.setStrategy = function(strategy) {
    this.strategy = strategy;
}
Bonus.prototype.getBonus = function() {
    return this.strategy.calculate(this.salary);
}

//使用
var Bonus = new Bonus();
bonus.setSalary(1000);
bonus.setStrategy(new performanceS());
bonus.getBonus();
</code></pre><h3 id="javascript版本"><a href="#JavaScript版本" class="headerlink" title="JavaScript版本"></a>JavaScript版本</h3><p>在JavaScript中，函数即对象，因此我们可以直接把strategy定义成函数:</p>
<pre><code>var strategies = {
    &quot;S&quot;: function ( salary ){
        return salary * 4;
    },
    &quot;A&quot;: function ( salary ){
        return salary * 3;
    },
    &quot;B&quot;: function ( salary ){
        return salary * 2;
    }
};

var calculateBonus = function(level,salary){
    return strategies[level](salary);
};

console.log(calculateBonus(&apos;S&apos;, 2000); //80000
console.log(calculateBonus(&apos;A&apos;, 1000); //30000
</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;定义：定义一系列算法，把他们一个个封装起来，并且使他们可以相互替换。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;需求：绩效为S的人年终奖有4倍工资，绩效为A的人年终奖有3倍工资，绩效为B的人年终奖有2倍工资。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var 
    
    </summary>
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/categories/javascript/"/>
    
    
      <category term="javascript" scheme="http://ronghuaxueleng.github.io/tags/javascript/"/>
    
  </entry>
  
</feed>
